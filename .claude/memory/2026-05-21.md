# 2026-05-21 — Brief 8 / CSCI-44 target-catalog landed + memory hygiene follow-up

## コンテキスト

- 前回までに Brief 8 は CSCI-41 / CSCI-43 / CSCI-42 / canonical-form refactor
  (PR #85) まで landed 済みで、残りは CSCI-44 `semantic-ci target-catalog`
  の 1 ピースだった。
- 本セッションでは CSCI-44 を実装し、PR #86 として draft PR を作成した。
- CSCI-44 の目的は Authoring meta surface を CLI に追加し、 target /
  operator / template / match schema を machine-readable JSON と
  human-readable text の両方で参照できるようにすること。
- 位置づけは verdict 不参加の Advisor / Authoring surface。 `check` /
  `compare` / `pre-commit` の verdict-bearing path に catalog module を
  混入させないことが INV-2 の主眼だった。
- catalog は target.yaml authoring を AI assistant / IDE extension / 外部
  authoring tool が正しく行うための reference surface。 user-facing には
  `semantic-ci target-catalog` として公開。
- CSCI-44 merge で Brief 8 は 4/4 landed。 ABCD-B 軸は完走。
- PR review では code 側 P2 1 件(open path filter)と memory hygiene P2 3 件が残った。
- code 側は commit `7e7895a` で修正済み。 memory hygiene は本 follow-up で
  STATUS / index / session log を整える。

## 設計判断

- catalog builder は `authoring/catalog.py` に置いた。
- registry 値は catalog 内で hard-code せず、 runtime registry を再 import して mirror する。
- 参照元は `compiler/path_schema.py`、 `compiler/type_schema.py`、
  `framework/match_schema.py`、 `compiler/templates.py`、
  `framework/constraint_types.py` / `compiler/operator_schema.py`。
- JSON envelope は `schema_version="catalog-1"` / `subcommand="target-catalog"`。
- default は full catalog。 `--kind` と `--target-path` は明示 opt-in filter。
- `--kind` は templates セクションだけを narrow する。
- `--target-path` は targets セクションだけを narrow する。
- `--kind` と `--target-path` は independent narrow として併用可能。
- unknown `--target-path` は usage error 2 + did-you-mean。
- `--target-path python_specific.value` のような open path は valid path として許可する。
- open path は `kind=open` / `category=unknown_open` として出力する。
- operator evidence_emits は static hard-code ではなく、 `evaluator/operators.py` の
  AST を走査して `_evidence(**{...})` kwargs から抽出する。
- この AST 走査は CSCI-43 / CSCI-42 の「producer actual output shape を grep してから
  validator を書く」教訓を直接反映したもの。
- `Operator.CHANGED_ONLY_IN` は P1 unsupported として `class="skipped_in_p1"`。
- human formatter は target.yaml authoring user が読む前提で、 primary_kinds /
  targets / templates / operators の 4 セクションに分けた。
- JSON schema は `src/semantic_ci_code/schemas/target_catalog.schema.json` として追加した。
- architecture tests では INV-1 / INV-2 / INV-4 / INV-5 を CSCI-44 に拡張した。
- INV-2 は `semantic_ci_code.authoring` prefix match により `authoring.catalog` も
  自動 cover される。
- PR #85 の canonical helper contract は catalog 内でも継承する。
- `authoring/canonical.py` は producer spec の単一 source of truth として残す。
- CSCI-44 は Brief 8 の最後の実装 PR。次の normal implementation queue は
  Brief 7 / SSP v0.1。

## 成功パターン

- registry を hard-code しない方針にしたことで、 enum / template / match schema
  追加時に parity test が即 fail する構造になった。
- target/operator/template/match_schema の catalog 内容を runtime registry から
  組み立てたため、 docs drift ではなく implementation drift を検出できる。
- `--kind` / `--target-path` の independent narrow を CLI test で固定した。
- dogfood として TC1 の refactor template constraint set が catalog から再構成可能で
  あることを cross-test で固定した。
- `semantic-ci target-catalog --format json` は `catalog-1 target-catalog 48 4 20`
  の形で期待件数を出した。
- `semantic-ci target-catalog --format human --kind feature --target-path
  api_surface_delta.added` で templates と targets の independent narrow を確認した。
- invalid target path は exit 2 + did-you-mean を返した。
- open path dogfood で `python_specific.value` が `kind=open` / `category=unknown_open`
  になった。
- `ruff check .` / `ruff format --check .` が通った。
- full pytest は `1258 passed, 4 skipped, 1 deselected in 133.35s` で通った。
- review fix 後の targeted test は `22 passed` で通った。
- GitHub Actions は 3.11 / 3.12 / 3.13 green。
- PR body には Completion Summary から始まる形で dogfood と validation を記載した。

## 修正・訂正

- 初回実装では `--target-path python_specific.value` が usage error になっていた。
- 原因は target catalog の path filter が `valid_target_paths()` のみを見ており、
  open path を認めていなかったこと。
- 修正は `is_open_path()` を併用し、 valid static path または open path を受け付ける形にした。
- 修正後、 unknown path の did-you-mean は維持した。
- code 側 review fix commit は `7e7895a fix(catalog): allow open target path filters`。
- memory 側 review では STATUS.md `## Phase` に新旧 paragraph が重複していた。
- 新 paragraph は Brief 8 完走を書いていたが、旧 paragraph は ABCD-B 3/4 landed /
  9 subcommand のままだった。
- `次の発行順序` も CSCI-44 paste 待ちのまま残っていた。
- `_index.md` に 2026-05-21 の CSCI-44 entry が無かった。
- 本 follow-up で `## Phase` を単一 canonical paragraph に統合した。
- 本 follow-up で `次の発行順序` の B heading / B-4 / 直近最短経路を Brief 8 完走状態に更新した。
- 本 follow-up で `_index.md` に 2026-05-21 row を追加した。
- 本 follow-up でこの `2026-05-21.md` session log を新設した。
- P3 nits として挙がった `applies_to_collection` naming / `_target_kind` lossy /
  evidence_emits hard-coded base sets は別 future PR 扱い。本 follow-up では触らない。

## 工程サマリー

1. CSCI-44 brief を受け、 branch `codex/csci-44-target-catalog` で実装開始。
2. `authoring/catalog.py` を新設し、 runtime registry mirror の catalog builder を実装。
3. `cli/commands/target_catalog.py` を追加し、 `semantic-ci target-catalog` subcommand を登録。
4. JSON / human formatter を追加。
5. `target_catalog.schema.json` を追加。
6. CLI integration tests を追加。
7. INV-5 parity tests を追加。
8. surface isolation / no-network import closure tests を CSCI-44 module 群に拡張。
9. dogfood cross-test を追加。
10. docs cross-ref を更新し、 Brief 8 完走扱いにした。
11. dogfood command を実走し、 expected counts / filters / did-you-mean / open path を確認。
12. `ruff check .` / `ruff format --check .` / full pytest を実行。
13. commit `e75bb9f feat(brief-8): add target-catalog command` を作成し push。
14. draft PR #86 を作成。
15. review comment に従い open path filter を修正。
16. targeted test 22 件を再実行。
17. commit `7e7895a fix(catalog): allow open target path filters` を push。
18. memory hygiene review 3 件を受け、 STATUS / index / session log の follow-up を実施。

## 成果物

- PR #86: `feat(brief-8): land CSCI-44 target-catalog authoring surface`。
- 新規 CLI: `semantic-ci target-catalog`。
- 新規 builder: `src/semantic_ci_code/authoring/catalog.py`。
- 新規 output: catalog JSON / human formatter。
- 新規 schema: `src/semantic_ci_code/schemas/target_catalog.schema.json`。
- 新規 CLI tests: default JSON / human / filters / output file / usage error / determinism。
- 新規 architecture tests: catalog implementation parity / verdict bytes invariant / surface isolation / no-network。
- docs updates: `cli_usage.md`、 `exit_codes.md`、 `json_schema.md`、
  `code_semantic_ci_design.md`。
- memory updates: `.claude/memory/STATUS.md`、 `.claude/memory/_index.md`、
  `.claude/memory/2026-05-21.md`。
- Dogfood evidence:
  - JSON catalog count: 48 targets / 4 templates / 20 operators。
  - `--kind feature` templates narrow。
  - `--target-path api_surface_delta.added` targets narrow。
  - `python_specific.value` open path accepted。
  - invalid target path did-you-mean。
- Validation:
  - `ruff check .` pass。
  - `ruff format --check .` pass。
  - `python -m pytest -q --no-cov` pass。

## 次セッションへの引き継ぎ

- 次の normal implementation queue は Brief 7 / SSP v0.1、 CSCI-36。
- 起草時必読:
  - `docs/brief_7_planning.md §11` checklist。
  - `AGENTS.md` Forward Design Note: Brief 7 / SSP v0.1。
  - `.claude/memory/STATUS.md` の `次の発行順序` C section。
- Brief 8 は CSCI-44 / PR #86 で完走扱い。
- ABCD-A + ABCD-B は landed。残りは ABCD-C (Brief 7 SSP) + ABCD-D (P2 残課題)。
- D follow-up として残る候補:
  - `docs/brief_8_planning.md §6.3.1` `ADVISORY-S1` narrow。
  - R17 deferred: `target-doctor --package-root` resolve consistency。
  - P3 nits: `applies_to_collection` naming / `_target_kind` lossy / evidence_emits base set。
- Brief 7 を始める前に PR #86 の merge 状態と memory follow-up commit の有無を確認する。
- `NO_COLOR=1` が環境に残っていると test expectation に影響することがあるため、
  full pytest 前は unset を確認する。
- CSCI-44 の catalog parity は registry drift detector として今後の enum / template 追加時に fail する前提。
- catalog failure は docs の stale ではなく implementation registry drift と見なす。

## メモ

- CSCI-44 は「AI が target.yaml を正しく書くための authoring meta surface」であり、
  verdict には参加しない。
- catalog は advisor surface だが、 runtime registry を読むため implementation と強く同期している。
- そのため INV-2 surface isolation と INV-5 implementation parity を同時に守る必要があった。
- `target-catalog` は target.yaml を必要としない。
- `target-catalog` は git / extractor / network を必要としない。
- `target-catalog` は LLM を呼ばない。
- `target-catalog` の value は registry mirror であり、 authoring UX の説明文を勝手に盛らない。
- JSON は external tools 向け、 human は人間 authoring 向け。
- Brief 8 完走により、 target.yaml authoring path は docs / doctor / init recipe / catalog の 4 面が揃った。
- ここから先の SSP は別軸。semantic-ci の core authoring loop は一段落。

---

# 2026-05-21 (Session 2) — R17 landed + 経験値外部化 framework 言語化 + CLAUDE.md / AGENTS.md 追記

## コンテキスト

Session 1 (CSCI-44 + memory hygiene follow-up) 完了直後の継続 session。 当初
プランは「次の一手」 として CSCI-44 paste 待ちを終えた Brief 7 (CSCI-36) 起草
だったが、 user の判断で **B (A 軸 follow-up = ADVISORY-S1 narrow) を先に
片付け** に変更 → これが既に 2026-05-15 commit `854a528` で完了済と判明 →
STATUS.md の stale entry 削除のみ (commit `bf8ce8f`) → 続いて **R17 deferred
(target-doctor `--package-root` resolve consistency) の brief 起草 → Codex
paste → PR #87 → 0 round merge** を実行。 後半は user との反省会で「Claude
単独 vs Codex 連携」 の比較 data point を分解、 **「経験値の外部化」**
framework を言語化し、 `CLAUDE.md` (軽) + `AGENTS.md` (substantial、 §5 新設)
への追記で永続化した。

セッション末尾は user の「今日はここまでにしよう」 wrap-up trigger に応じて
本 session log + `_index.md` + STATUS.md を memory exception で direct main
push する仮まとめで closure。

## 設計判断

1. **B (S1 narrow) を先に確認したのが正解** — Brief 7 起草へ即進む推奨に
   対して user が B を選択 → 「実は既に landing 済」 が判明、 STATUS.md
   の 3 sites stale entry を削除する 5 分タスクで closure。 もし Brief 7
   に直行していたら、 §B-2 follow-up に「ADVISORY-S1 narrow 残」 を抱えた
   まま起草を始めていた。 user の「先に整地」 判断が memory hygiene 上
   合理的だった
2. **R17 brief は明示的に小さく書いた** — 「Goal: 1 関数置換 + 5 test 追加」
   レベルまで narrow、 Implementation Hints に具体コード片を逐語埋め込み、
   既存の check.py:205-219 / pre_commit.py / validate_plan.py の 3 surface
   既存 pattern を「parity」 文言で逐語参照。 結果 Codex は brief 通り
   実装 + brief に書かれていない symlink test を **自発的に追加** (brief
   §11 R1 / §15.6 の invariant scope narrowing pattern と整合)
3. **`_resolve_package_root` の base 選択 logic** — `is_git_available()` →
   `repo_root(Path.cwd())` の二段ガードで GitError も catch、 non-git
   directory での fallback を保持。 target-doctor は git なしでも
   D1/D3/P1/P2/S1 advisory を出せる surface なので fallback 必須。 D4
   advisory のみ git 必要だが `_resolve_files_touched` 側で別途ガード済
4. **CLAUDE.md は軽く、 AGENTS.md は厚く** — user の指示通り、 CLAUDE.md
   は principle 1 paragraph + 4 item bullet + AGENTS.md への pointer で
   ~25 lines に narrow、 AGENTS.md は新 §5 として 8 sub-section (5.1
   Principle / 5.2 Empirical Envelope / 5.3 Three-Tier Externalization /
   5.4 Round Count as Indicator / 5.5 体制別 envelope / 5.6 Maintenance
   Practice / 5.7 Anti-Patterns / 5.8 Cross-Reference) で ~180 lines。
   AGENTS.md は handoff protocol の延長として discipline 規律を体系化、
   新 brief 起草前 / 新 architectural pattern 導入前の必読 doc に位置付け
5. **経験値外部化 framework の base case は PR #82 → #84 → #86/#87** —
   16 + 13 = 29 round 累計 P2 を `test_canonical.py` 48 cases +
   `tests/architecture/` 16 tests に encode した結果として 0 round が
   実現された因果を、 empirical envelope として §5.2 表に明示。 単なる
   「速くなった」 ではなく「外部化 work が round 数を測定可能に押し下げ
   た」 という観察。 普通の dev における「ベテランの暗黙知」 が、 Claude
   の long-term memory 不在制約下で **強制的に明示 artifact に置き換わる**
   逆説を §5.1 で言語化

## 成功パターン

1. **A 軸 follow-up の事前確認** — STATUS.md に残っていた「stale entry」
   が **実体としては既に landing 済** だった事例。 brief 起草前に
   `git log -p --all -- <file>` で対象 file の最新変更を確認する pattern
   が work した。 stale memory を信じて作業着手すると無駄な round が
   発生する
2. **R17 brief の Implementation Hints に逐語コード片を埋める** — Codex の
   review round 0 を狙うなら、 brief に「こう書け」 を逐語提示するのが
   最短。 既存 surface (check.py:205-219) を「parity 対象」 として逐語
   引用、 新 helper の skeleton を `_resolve_package_root` 内に直接書く
   ことで Codex の implementation pass を 1 round で確定
3. **AskUserQuestion で 4 択提示する pattern が再現** — 「次の一手」
   選択で「A (Brief 7) / B (A 軸 follow-up) / B → A 両方 / C (D 軸 P2)」
   の 4 択提示 → user 判断 1 ターンで B 確定。 PR #84 round 10、 5/19
   canonical refactor 判断、 本 session で 3 連続実証 = pattern として
   stable
4. **会話的振り返り → framework 言語化 → docs 永続化の流れ** — user
   観察 「Codex 連携の方がタスク進む」 → 実測 data 比較 (PR #82/#84/#85/
   #86/#87 の round 数表) → 「経験値の外部化」 framework に抽象化 →
   user 「ここまでの経験値が生きている」 → CLAUDE.md / AGENTS.md への
   永続化。 会話で気付いた pattern を即座に外部化する loop が本 session
   で実演された (本 session 自体が経験値外部化 discipline の自己言及)

## 修正・訂正

1. **当初 STATUS.md `次の発行順序` に R17 を「deferred」 で残置していた** —
   Codex が PR #87 で 3 sites の R17 entry を**全て**完走表記に書換、
   私が手動で別途修正する必要がなかった。 Codex が follow-up commit を
   独立に起こすパターンが 5/19 (canonical refactor) と本 session の R17
   で再現、 brief に「STATUS.md 更新を Scope IN に明示」 することの
   重要性が再確認された
2. **session continuity の取り扱い** — 本 session log を「Session 2」 と
   して 2026-05-21.md 末尾に追記する形式を採用 (CSCI-44 follow-up の
   Session 1 を保持)。 5/15 Session 4 と同様、 同日複数 session 運用が
   2 連続で発生、 索引上は 1 行に圧縮するか別行にするかは「session 間に
   独立した PR landing が挟まったか」 で判断 (本日 PR #87 が挟まったので
   Session 2 として別 entry)
3. **`_index.md` の Session 1 entry に「(本 follow-up で新設)」 と書いて
   いた箇所** — Codex が 2026-05-21.md を新設したのは事実だが、 Session 2
   側でも継続執筆するため文言整合の必要性。 ただし Session 1 の文章を
   遡って書換えるのは memory log の immutability 原則に反するので、
   Session 2 entry 側で「Session 1 に追記」 と明示する形にした

## 工程サマリー

| 工程 | 内容 | 成果 |
|---|---|---|
| 1. PR #86 merge 確認 | webhook + git pull で main 同期、 Codex follow-up `623a40c` 確認 | ABCD-B 完走認識、 _index.md / STATUS.md state 確認 |
| 2. 次の一手提示 | 4 択 (A Brief 7 / B S1 narrow / B→A / C D 軸) | user 「B」 確定 |
| 3. S1 narrow 調査 | grep + git log で「既に commit 854a528 (2026-05-15) で完了済」 と判明 | 想定外: 既に landing 済 |
| 4. STATUS.md stale 削除 | 3 sites の「A 軸 follow-up 残」 entry を削除、 完走 ref に書換 | commit `bf8ce8f` 直 push |
| 5. R17 brief 起草 | §15 checklist 実走 + check.py:205-219 grep + Implementation Hints に逐語コード | paste-ready brief 完成 |
| 6. PR #87 review | CI 3/3 green、 1269 passed、 6 tests 追加 (brief 要求 5 + bonus symlink) | 0 round merge ready 判定 |
| 7. PR #87 merge | user merge button | commit `469385e` main landed |
| 8. 反省会 | Claude alone vs split の比較 → data 表で empirical envelope 提示 | 「経験値の外部化」 framework 言語化 |
| 9. CLAUDE.md / AGENTS.md 追記 | CLAUDE.md 軽 (~25 lines) + AGENTS.md §5 substantial (~180 lines 8 sub-section) | 経験値外部化 discipline を policy / handoff doc に永続化 |
| 10. 仮まとめ | Session 2 log 追記 + `_index.md` Session 2 entry + STATUS.md refresh | 本 commit |

## 成果物

- **PR #87** (merged `codex/r17-target-doctor-package-root-parity` → main、
  commit `469385e`):
  `fix(target-doctor): resolve --package-root against repo root (parity
  with check)`
  - `src/semantic_ci_code/cli/commands/target_doctor.py` (+31/-1):
    `_resolve_package_root` を repo-root aware 版に置換、 absolute reject /
    `..` escape reject / symlink defense / `repo_root(Path.cwd())` with
    cwd fallback の 4 layer 実装
  - `tests/cli/test_target_doctor.py` (+218/-1): 6 tests 追加 (subdir
    parity / `.` parity / D4 filter parity / abs reject / parent escape /
    git unavailable fallback) + bonus symlink reject test (Windows skip)
  - `.claude/memory/STATUS.md` (+7/-8): 3 sites の R17 deferred entry を
    完走表記に書換
- **commit `bf8ce8f`** (direct main、 memory exception):
  `memory: mark ADVISORY-S1 narrow follow-up as already landed (commit
  854a528)`
  - STATUS.md の 3 sites stale entry を削除 (§A ResultStatus split
    follow-up / §B-2 CSCI-43 follow-up / 直近最短経路)
- **commit (本 wrap-up)** (direct main、 memory + policy exception):
  - `CLAUDE.md` に Experience Externalization (経験値の外部化) section
    新設 (軽、 ~25 lines、 AGENTS.md §5 への pointer)
  - `AGENTS.md` § 5. Experience Externalization Discipline 新設
    (substantial、 ~180 lines、 8 sub-section: Principle / Empirical
    Envelope / Three-Tier Externalization / Round Count as Indicator /
    体制別 envelope / Maintenance Practice / Anti-Patterns /
    Cross-Reference)
  - `.claude/memory/2026-05-21.md` に Session 2 追記
  - `.claude/memory/_index.md` に Session 2 entry 追加
  - `.claude/memory/STATUS.md` に R17 完走を 直近 merged に追記、
    直近最短経路 narrow

## 次セッションへの引き継ぎ

### 最優先 = Brief 7 (CSCI-36) entry

- Codex 起動可能日に着手。 `docs/ssp_protocol.md` v0.1 spec を新設
- docs only spec 起草 (500-700 行)、 Claude 単独完結可
- 起草時必読:
  1. `AGENTS.md` Forward Design Note: Brief 7 / SSP v0.1 (旧 §, 本 session
     で `## 5. Experience Externalization Discipline` が挟まったので
     section 番号 / 位置に注意)
  2. `docs/brief_7_planning.md §11` 着手 checklist
  3. **AGENTS.md §5.6 Maintenance Practice 7 つの rule を遵守** (brief
     起草前 memory 読み返し + §15.1 Schema grounding + AskUserQuestion
     N 択提示 等、 本 session で永続化した規律を逐語適用)
- 起票 task: SSP tracking GitHub issue (Issue #48 close 後の受け皿、
  `brief_7_planning.md §11` で text template 固定済)

### deferred / 軽い follow-up

- **D 軸 (P2 残課題)** brief 起草: D-1 Lock 即 fail / D-2 Perf budget /
  D-3 Hash trail。 いずれも brief 起草が必要、 中規模。 ABCD 完走への
  必須路、 ただし Brief 7 と並列発行可
- **post-ABCD external readiness phase** planning: `docs/multi_agent_
  audit_case.md` と並列の `ai_velocity_accumulation_case.md` (仮) 起草
  検討 — ABCD 完走時点で「Claude exception vs split vs Claude small」
  の 3 mode 比較 + 4-tier empirical envelope を case study として永続化、
  本 session で言語化した経験値外部化 framework の実証データ集約

### 規律 maintenance

- AGENTS.md §5 が新設されたので、 **次回新 brief 起草前に §5 全 sub-section
  を逐語参照** すること (これは §5.6 #7 の自己言及 = 本 doc 自身が brief
  起草前の必読参照対象)
- §5.7 Anti-Patterns 7 件は brief 起草中 / review 中に違反していないか
  self-check

## メモ

- 本 session は **「経験値外部化 discipline」 を自己言及的に実演した**
  session。 反省会で気付いた pattern を即座に CLAUDE.md / AGENTS.md に
  encode する流れ自体が、 言語化した規律の自己適用例として記録に値する
- AGENTS.md §5 が **brief 起草前必読** に位置付けられたことで、 次セッション
  以降の brief 起草 workflow が更新される: memory 読み返し → §15 checklist
  → §5 (新規) の 3 段の前提確認になる
- 5/15 Session 4 の 13 round が **「外部化が間に合っていなかった」** 状態
  だったことが §5.2 表で empirical 化された。 単なる「失敗」 ではなく
  「経験値が test/docs に encode される前の状態」 と framing し直すと、
  29 round 累計が後続 PR の 0 round に転じた因果が明確になる
- ABCD-B 軸が完走、 ABCD-C (Brief 7 SSP) + ABCD-D (P2 残課題) の 2 軸が
  残務。 **「ゲームとしては完成 (クローズドβ相当)」** の到達は ABCD
  完走時点なので、 あと 5 + 3 = 8 PR 程度の道のり (見積もり)
- 「Codex 連携の方がタスクが進む」 user 観察は実測と一致するが、 厳密に
  は **「規律 infrastructure 揃った状態の split 運用 + Codex implementation
  discipline」 が 1 日規模 brief を 0 round で消化する empirical envelope**
  という framing が正しい (§5.5)。 「Codex が intrinsically 速い」 でも
  「split が魔法」 でもなく、 brief 起草と外部化規律が前提条件
- 本 session 中の AskUserQuestion は 1 回のみ (次の一手 4 択)。 4 択 →
  user 1 ターン判断の pattern が pattern として stable、 brief 起草中の
  「continue / pivot / drop」 系判断も同形に展開可能
