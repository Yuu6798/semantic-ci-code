# 2026-06-09 — Phase H 始動: H-2b advisory re-projection 完走 (CSCI-51b / PR #144)

## コンテキスト

Phase H (LLM security scout layer) が gate 解除後に始動し、本 session は
その 3 番目のスライス H-2b (CSCI-51b、deterministic one-run advisory
re-projection) を **brief 起草 → PR #144 review → merge** まで通した。
H-1 (CSCI-50 / PR #142、`LLMSecurityFinding` + `project_to_canonical` +
`LLMSensorProvenance` + verdict reject guard) と H-2a (CSCI-51 / PR #143、
`CodeStateDelta.renames` + `RenameEntry` の CLI overlay) は本 session 前段
(summarized context) で landing 済。本 session の主眼は H-2b の設計確定と
review。

H-2b の核命題: LLM scout が candidate で炙り出した `LLMSecurityFinding` を
**LLM を再実行せず** baseline `CodeState` に対する決定論述語で「新規
(added) / 既存 (pre-existing)」に分類する advisory-only エンジン。出力
`AdvisoryReprojection` は verdict 経路から構造的に隔離 (D1「LLM は scout で
あって judge ではない」)。

## 設計判断

1. **baseline 述語の深さ = A (decorator-based absence concrete)** を user が
   確定。absence anchor のみ `api_surface.decorators` を見る小さな固定表
   (`missing-authz` → `{login_required, requires_auth, permission_required,
   authenticated}`) で判定し、presence / 未知 class / site 解決不能は全部
   D7 fallback で added に倒す。ガードレール (固定表は小さく / advisory 専用 /
   heuristic・recall-first を docs/test に明記) を AC4/AC6 + doc note に encode。
2. **§15.1 grounding-first**: brief 起草前に merged H-1/H-2a symbol
   (`LLMSecurityFinding` 9 要素 identity / `project_to_canonical(finding, *,
   sensor_id)` / `APISurfaceEntry.decorators` / `RenameEntry` / `_decorator_leaf`
   規約 / `compute_security_delta` reject) を逐語確認してから AC 化。fqn↔path
   変換 (rename site 解決) だけは暫定とし AC は behavioral に保った。
3. **verdict 隔離を型 + import の二重で固定**: `AdvisoryReprojection` ≠
   `SecurityDelta`、`suite` は `sensor.advisory` を import しない (transitive-
   closure architecture test)。

## 成功パターン

- **AskUserQuestion で述語の深さ (A/B/C) を先に確定** → brief が 1 発で
  landing、PR #144 review もバグ 0。Phase G で確立した「設計フォークを
  AskUserQuestion で pin → 各 brief 1 発 landing」の再現。
- **review で深掘り 2 点を自力検証 → どちらも問題なしと確認**: (a)
  `_decorator_leaf` の recall-first 方向 (decorator-with-args の取りこぼし
  懸念) → `api_surface/python_api_extractor.py:302-303` が `ast.Call` を
  `node.func` に unwrap して call args を捨てる事を確認、コメント主張が正確
  と裏取り。(b) `compute_security_delta` の raise が CLI で graceful か →
  `check.py:300` の `except ValueError → _usage_error` (exit 2) で着地、
  traceback crash しない事を確認。

## 修正・訂正

1. **review 後に Codex が 4 follow-up commit を追加** (review 時点の単一
   commit 96bd855 → merge 時 5 commit / +521 行)。enclosing-class auth guard
   考慮 (`0ba4c24`) / package-root prefix rename (`431ea49`) / **advisory を
   `sensor/__init__.py` export から除外** (`4248dab`、isolation 強化、私が
   review で見た export を撤回) / 重複 API entry 保持 (`74778f1`)。私の review
   は初版 commit ベースだったので、これらの refinement は merge 直前に追加
   された (review → land の間で iterate)。
2. **scope 申し送り (非ブロッキング)**: `compute_security_delta` が LLM
   finding を silent filter → `ValueError` raise に変えた点は brief OUT-scope
   「verdict 配線は変更しない」を技術的に跨ぐが、fail-closed 強化として受容。
   `--candidate-sensor` 経由の挙動が silent-pass → usage-error に変わった事は
   いずれ `docs/exit_codes.md` か `cli_usage.md` に 1 行残す価値あり (H-4 の
   CLI 配線時で可)。

## 工程サマリー

| 工程 | 成果 |
|---|---|
| H-2b brief 起草 (CSCI-51b) | A=decorator-based + ガードレールを AGENTS.md §1 format で emit、§15 ゲート closeout |
| PR #144 review | 8 AC 全充足を検証、深掘り 2 点を自力裏取り、APPROVE + scope 申し送り 1 件 |
| merge | user が Codex 👍 後に merge (4 follow-up commit 込み)、main = dadbf1b |

## 成果物

- **PR #142** (CSCI-50 / H-1): LLM advisory finding protocol (本 session 前段)
- **PR #143** (CSCI-51 / H-2a): git rename map CLI overlay (本 session 前段)
- **PR #144** (CSCI-51b / H-2b): `sensor/advisory.py` =
  `compute_advisory_reprojection` + `AdvisoryReprojection`、architecture test
  (one-run contract / suite-advisory isolation)、`compute_security_delta` の
  fail-closed reject 化、planning doc 同期。merged 2026-06-09。

## 次セッションへの引き継ぎ

- **H-3 (CSCI-52)**: Codex Security concrete adapter + fixture mode (記録済み
  出力 ingest)。LLM-general Adapter Protocol の first concrete。on-demand 実行。
- **H-4 (CSCI-53)**: advisory surface 出力 + mute ledger + CLI `--sensor-advisory`
  配線 + informed-consent provenance。ここで上記 scope 申し送り (silent-pass →
  usage-error の doc 化) を回収。
- **H-5 (CSCI-54)**: 残スライス (planning §3 参照)。
- repo-internal の D-class closure (D6/D7/D8) は依然 ROADMAP v0.1.0 exit
  criteria の前進候補。

## メモ

- Phase H は「candidate / active 投入は要判断」から **active** に遷移済
  (H-1/H-2a/H-2b merged)。STATUS.md Phase + 次の発行順序を本 wrap-up で更新。
- review は GitHub に投稿せず chat 内で完結 (CLAUDE.md workflow の Claude=
  design/review role、frugal GitHub policy)。
