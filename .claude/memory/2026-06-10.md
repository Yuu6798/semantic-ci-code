# 2026-06-10 — Phase H 完走 (H-3/H-4/H-5 = PR #146/#147/#148 を 1 session で 3 連続 landing)

## コンテキスト

朝のタスク確認から始まり、STATUS.md の active queue (Phase H 継続) に沿って
H-3 → H-4 → H-5 の 3 スライスを「brief 起草 (new-brief skill §15 gate) →
Codex 実装 → chat 内 review → user merge → STATUS sweep」のサイクルで連続
消化。**Phase H (LLM security scout layer、CSCI-50〜54) が本日で全完走**。
全 PR が review 0 round (chat 内 APPROVE、ブロッキング指摘ゼロ) で landing し、
P3 指摘も follow-up commit で即日消化された。

## 設計判断

1. **H-3 (CSCI-52): live LLM 呼び出しを in-repo 経路に置かない** — CLAUDE.md
   no-LLM/no-network 規約 + AGENTS.md escalation rule 5 から、Codex Security
   adapter は fixture-mode ingest (記録済み envelope) のみを実行経路とし、
   D1 の on-demand は「呼ばれない限り何も走らない」構造で充足。no-network /
   no-subprocess を architecture test (`test_llm_adapter_no_network.py`) で構造化。
2. **H-4 (CSCI-53) Q3 = mute ledger は別ファイル** (`.semantic-ci/advisory_mutes.yaml`
   + 明示 `--advisory-mutes`、auto-discovery なし)。user に噛み砕き説明後
   no-preference → 推奨案で確定。根拠は「declared intent (freeze 対象の契約書) と
   既読メモを構造分離」— scout-not-judge と同型の判定参与/非参与の構造分離。
3. **H-4: `Suppression` 再利用不可の発見** (§15.1 grounding の成果) — 既存
   `Suppression` の identity validator は sast/sca のみ受理。planning §2.4 の
   「踏襲」はフィールド構成の踏襲であり、llm category (9 要素) 専用の
   `AdvisoryMute` 新設に落とした。
4. **H-5 (CSCI-54) Q4 = (a) 明示的束ね役関数** (AskUserQuestion で user 確定)。
   ensemble は固定 sensor_id `llm-ensemble` で再射影 (member 構成が変わっても
   mute key 安定)、member provenance は SensorState の **外** (`members`) に保持
   — `_validate_state_invariants` の「provenance dict key = sensor_id」制約により
   同一 adapter の複数 payload は 1 state に同居できない、という実装制約からの必然形。
5. **D9 informed-consent は counts + members で realize** — finding 単位の
   「宣言 M 件」マッチングは未定義のため counts (scouted/surfaced/pre_existing/
   muted) + muted audit 記録 + ensemble members[] に絞った。
6. **昇格経路 doc は新設 `docs/llm_scout_usage.md`** (ACTIVE) — finding class →
   G-5 security recipe 対応表 / 沈黙=容認 (D8) / mutes vs suppressions 対比 /
   ensemble 時の mute key 注意を集約。

## 成功パターン

- **§15 gate の grounding が review 0 round に直結** (3 brief 連続): 実装制約
  (Suppression validator / SensorState invariants / check.py の renames 既算) を
  brief 起草前に grep で確定 → AC が実装と一度も衝突しなかった。
- **AskUserQuestion の前に噛み砕き説明を挟む**: Q3 で「差分がわからない」と
  返ってきた際、契約書/既読メモの比喩で説明し直してから再提示 → Q4 では
  説明込みの一発回答が得られた。trade-off 説明の解像度を user に合わせる。
- **merge 直後の STATUS sweep を毎回即実行** (discipline test gate 込み直 push):
  stale entry 蓄積の anti-pattern を 3 merge とも回避。
- **review は AC 番号で機械的に照合 + ローカル full test 実測** (1681/1689 pass
  を都度確認)。P3 は「挙動バグでない」ことを明示して merge を止めない判断を明確化。

## 修正・訂正

- 大きな誤りなし。P3 指摘 3 件 (H-3: dummy CodeState 型 / 混在 ordinal、
  H-4: "later H-4 slice" 文言 + 未消費 tuple 要素) はいずれも follow-up commit
  または次スライスで消化済み。
- H-5 review で観測 3 件を申し送り化 (下記「次セッションへの引き継ぎ」)。

## 工程サマリー

| 工程 | 成果 |
|---|---|
| H-3 brief 起草 + review | PR #146 (Codex Security fixture-mode adapter)、AC 10 件全充足、P3 ×2 は follow-up commit で消化 |
| H-4 brief 起草 (Q3 確定) + review | PR #147 (advisory surface + mute ledger + CLI 配線)、AC 15 件全充足、H-2b/H-3 申し送りの silent-pass→usage-error doc 化回収 |
| H-5 brief 起草 (Q4 確定) + review | PR #148 (llm-ensemble 明示集約 + `docs/llm_scout_usage.md`)、AC 14 件全充足、**Phase H 完走** |
| STATUS sweep ×3 | 各 merge 直後に discipline gate 込みで直 push |

## 成果物

- **PR #146** (CSCI-52 / H-3): `sensor/adapters/llm/codex_security.py` +
  no-network architecture test + fixtures
- **PR #147** (CSCI-53 / H-4): `sensor/mutes.py` + `check --advisory-sensor` /
  `--advisory-mutes` + envelope `advisory` object + exit_codes/cli_usage/
  json_schema doc 同期
- **PR #148** (CSCI-54 / H-5): `sensor/adapters/llm/aggregate.py`
  (`aggregate_advisory_states` / `llm-ensemble`) + 複数 `--advisory-sensor`
  解禁 + `advisory.members[]` + `docs/llm_scout_usage.md` 新設

## 次セッションへの引き継ぎ

- **Phase H 完走** → active queue の次候補は **D-class closure (D6/D7/D8、
  ROADMAP v0.1.0 exit criteria 前進)** または **Phase X (ecosystem cross-repo、
  別 session 委譲)**。
- H-5 review 申し送り (P3、いずれも非ブロッキング、着手は要判断):
  1. `aggregate_advisory_states` が非 LLM 入力 (deterministic provenance /
     SAST/SCA finding) を silent skip — fail-closed にするなら ValueError 化。
  2. ensemble の conflict 解決で severity と message の出所 member が分かれ得る
     (spec どおりだが不協和) — 「max severity の finding の message 優先」案。
  3. ensemble 時の `counts.scouted` は dedup 後件数 — D9 の「N 件炙り出し」の
     解釈として doc 1 行の価値。
- 2026-06-07 からの未 encode 課題 (navigate 実証: `check --candidate-source
  working-tree` の in-loop 検出 / `compare` 仮想スタブの生成前判定) は依然未回収。

## メモ

- 本 session は Claude Code on the Web (remote 環境) から実行。brief paste →
  Codex 実装 → PR URL 共有のサイクルが remote でも摩擦なく回ることを確認。
- 3 PR とも Completion Summary が AGENTS.md §2 完全形でない (per-AC checklist /
  Files Changed / Deviations 欠落) のが 2 回連続 → 3 回目。実害はないが、
  次回 brief の Required Outputs に「§2 形式逐語」と 1 行入れる価値はあるかも。

---

## Session 2 — セキュリティ hardening 緊急パッチ (PR #149、Fable タスク能力判定兼ねる)

### コンテキスト

user から「Fable にこの repo の脆弱性を診断してほしい」→ 脅威モデル (CI で動く本ツール
が信頼できない入力・git ref・sensor payload・PR 内容を処理する際の攻撃) を中心に repo
全体を診断。結果は **High/Critical なし**: 設計上の「LLM 非依存・ネットワーク非依存・
決定論」がそのまま攻撃面縮小に効いており、`eval`/`exec`/`pickle`/`marshal` の動的実行
なし・YAML 全箇所 `yaml.safe_load`・subprocess 全て list-arg (`shell=True` なし)・
cache key は sha256 hex (パストラバーサル不可)・ハードコード秘密情報なし。検出は
defense-in-depth レベル 2 件 + 情報レベル 2 件。続けて user 指示で「1 と 2 を緊急パッチ
として実装まで (Fable のタスク能力判定も兼ねる)」→ Claude exception (≤半日) で実装・
PR #149・subscribe・CI/review 対応・merge まで完走。

### 設計判断

1. **ref ガードは全 chokepoint に一括挿入** — `ensure_safe_ref()` を `git_runtime.py`
   に新設し、ref が git argv に渡る全経路 (`ref_exists`/`tree_object_id`/
   `resolve_baseline`/`resolve_candidate`/`materialize_ref`/`check._resolve_baseline_ref`)
   に挿入。空文字 or `-` 始まり (例 `--upload-pack=...`) を subprocess 起動前に拒否。
2. **`GitRefError` の二重継承で exit code routing を不変保持** — `GitRefError(GitError,
   ValueError)` にすることで、ValueError を usage error (exit 2) に流す check/observe と、
   GitError を git 経路 (exit 3) に流す target-doctor の両方が、追加の except 節なしで
   既存 routing を維持。これが今回の設計の肝。
3. **`pull_request_target` は test に encode** — SECURITY.md 新設は CLAUDE.md 表 +
   README 更新を誘発しスコープが膨らむため緊急パッチでは見送り、脅威モデルの 3 不変条件
   (PR checkout 禁止 / `${{ }}` の run 補間禁止 / read-only 権限) を workflow コメント +
   `tests/discipline/test_workflow_pull_request_target.py` (正例 + 4 負例) に encode。
   「経験値は test に外部化」という repo 推奨形に倒した。

### 成功パターン

- **テストが実装バグを 2 回先に捕まえた**: (a) argparse が space 区切りの dash 値を先に
  弾くため、ガードに届く形は `--candidate-rev=<値>` だと判明 → test を実態に合わせた。
  (b) `--baseline-rev` が `resolve_baseline` でなく `_resolve_baseline_ref` という別
  ヘルパーを通る抜け穴を、test の exit 3 失敗が暴いた (grep だけなら見落とす経路)。
- **PR subscribe 後の自律対応**: CI fail (絶対 import) と Codex P2 (空 ref fallback) を
  webhook 受信 → 原因特定 → 修正 → push → 緑確認まで、user 介入なしで回した。

### 修正・訂正

1. **CI fail = test の絶対 import** (`from tests.cli.git_helpers import`)。bare pytest
   (CI) は repo root を sys.path に載せないため `No module named 'tests'` で collection
   失敗。ローカルの `python -m pytest` では通っていた (cwd が載る)。既存 `tests/cli/`
   と同じ相対 import (`from .git_helpers import`) に修正。**教訓: 新 test file の import
   形式は同ディレクトリの既存 test に合わせる。`python -m pytest` 緑は bare pytest 緑を
   保証しない。**
2. **自分の workflow コメントが既存 test を破壊** — コメント中の文字列 `actions/checkout`
   が `test_pr_body_dogfood_disclosure.py` の素朴な substring チェックに引っかかった。
   文言を「no checkout step for the PR ref」に言い換えて解消 (意味保持)。
3. **Codex P2 = 空 ref の silent fallback** — `resolve_baseline`/`resolve_candidate` が
   truthiness 判定で、`--candidate-rev=` (空文字) を「未指定」扱いし黙って HEAD に
   フォールバック。exit_codes.md に書いた「空 ref → exit 2」と矛盾。`is not None` 判定に
   変更し、明示空 ref は `GitRefError` で拒否 (回帰 test 2 本追加)。check.py 側は既に
   `is not None` 済みだったが git_runtime.py に同パターンが残存していた。

### 工程サマリー

| 工程 | 成果 |
|---|---|
| repo 全体セキュリティ診断 | High/Critical なし、defense-in-depth 2 + 情報 2 を報告 (file:line 付き) |
| 緊急パッチ実装 | ref ガード + pull_request_target test、新規 test 2 file |
| PR #149 + subscribe | Completion Summary 完全形で起票、CI 監視 subscribe |
| CI/review 対応 | CI fail 1 (import) + Codex P2 1 (空 ref) を自律消化、1717 test 緑 |
| merge | user merge、main = b47e521 |

### 成果物

- **PR #149**: `cli/git_runtime.py` (`ensure_safe_ref` + `GitRefError`) /
  `cli/worktree.py` / `cli/commands/check.py` / `.github/workflows/pr-body-discipline.yml`
  (不変条件コメント) / `docs/exit_codes.md` (1 行) / `tests/cli/test_git_ref_guard.py` (新規) /
  `tests/discipline/test_workflow_pull_request_target.py` (新規)。

### 次セッションへの引き継ぎ

- セキュリティ診断の**情報レベル指摘 2 件**は未対応 (緊急でないため):
  (3) extractors の AST/tokenize DoS 面 — `--extractor-timeout` で緩和済、CI に timeout
  設定すれば十分。(4) semgrep/pip-audit subprocess の env 継承 — dev-only 依存、scope 外。
- **SECURITY.md** 新設は別 brief 候補 (CI 脅威モデルの明文化、CLAUDE.md 表 + README 更新込み)。
- active queue は不変: D-class closure (D6/D7/D8) / Phase X (別 session) / Phase H 申し送り P3。

### メモ

- 「Fable のタスク能力判定」という枠組みで、診断 → 実装 → PR → CI/review 自律対応 →
  merge の一連を完走。テストが実装バグを 2 回先回りで捕まえた点と、exit code routing を
  型の二重継承で不変保持した設計判断が、今回の手応えのある部分。
