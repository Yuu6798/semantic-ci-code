# Session Memory Index

| Date | Summary |
|------|---------|
| 2026-05-21 (Session 2) | **R17 (target-doctor `--package-root` parity) landed (PR #87) + 経験値外部化 framework 言語化 + CLAUDE.md / AGENTS.md 永続化**。 Session 1 (CSCI-44 + memory hygiene) 直後の継続 session。 当初プランの Brief 7 (CSCI-36) entry に対して user が B (A 軸 follow-up = ADVISORY-S1 narrow) 先行を選択 → 調査の結果 **既に 2026-05-15 commit `854a528` で landing 済** と判明 (STATUS.md の 4 sites stale entry が原因)、 stale 削除 commit `bf8ce8f` direct main push で 5 分 closure。 続いて **R17 deferred** (target-doctor `--package-root` を `check` と parity な repo-root 相対 resolve に揃える) の brief を起草、 Implementation Hints に check.py:205-219 の逐語コード片を embed、 Codex paste → PR #87 → CI 3/3 green / 1269 passed / 6 tests + bonus symlink reject / 0 round merge ready → merge `469385e`。 後半は user との反省会で **「Claude alone vs Codex 連携」** の比較 data を分解、 PR #82 (16 round) / #84 (13 round / Claude exception) / #85 (0 round / Claude small scope) / #86 #87 (0 round / split) を empirical envelope 表として整理、 **「経験値の外部化」** framework を言語化: AI 開発は session 跨ぎの暗黙知を継承しないので docs / tests / checklists / pattern catalog に **強制的に外部化** することで再現性を維持する原則を確立。 永続化: `CLAUDE.md` に Experience Externalization (経験値の外部化) section 軽 (~25 lines) + `AGENTS.md` § 5. Experience Externalization Discipline substantial (~180 lines、 5.1 Principle / 5.2 Empirical Envelope / 5.3 Three-Tier Externalization (codified / repo-specific / session-tacit) / 5.4 Round Count as Leading Quality Indicator (0/1-3/5-10/10+ の解釈表) / 5.5 体制別 envelope (split / Claude alone / Claude exception) / 5.6 Maintenance Practice 7 rule / 5.7 Anti-Patterns 7 件 / 5.8 Cross-Reference) を追記、 新 brief 起草前 / 新 architectural pattern 導入前の必読 doc に位置付け。 **設計判断**: (1) 経験値外部化の base case = PR #82 + #84 の 29 round 累計 P2 を `test_canonical.py` 48 cases + `tests/architecture/` 16 tests に encode した結果として後続 0 round 達成、 因果関係を §5.2 表で明示、 (2) Claude long-term memory 不在制約が **強制的 externalization discipline** として逆説的に働く framing を §5.1 で言語化、 (3) review round 数を leading quality indicator として運用する原則 (§5.4)、 (4) 体制別 envelope (§5.5) で「split は規律 infrastructure 揃いで初めて 0 round 出る、 Codex が intrinsically 速いわけではない」 を明示、 (5) Maintenance Practice 7 rule + Anti-Patterns 7 件 (§5.6 / §5.7) で「規律を壊さない」 ための operational rule を逐語化、 (6) 「ベテランの暗黙知」 が AI 開発では機能しない、 個人 / agent 閉じ込めから外部化 artifact への置換が必須という principle を CLAUDE.md / AGENTS.md 両方に pin。 **修正・訂正**: A 軸 follow-up 「残」 表記が stale だった (実体は 5/15 完了済)、 STATUS.md の memory hygiene が PR landing 後の追従漏れで発生 → §5.7 Anti-Patterns #7 「STATUS.md `次の発行順序` 更新を後で先送り」 として明示化。 ABCD-B 軸 4/4 完走 + canonical refactor follow-up + R17 deferred 全て landed、 残るは ABCD-C (Brief 7 SSP / 5 PR) + ABCD-D (P2 残課題 / 3 brief)。 次セッションは Brief 7 / CSCI-36 entry (`docs/ssp_protocol.md` v0.1 spec 起草、 docs only、 Claude 単独可)、 起草前必読が **§15 brief drafting checklist + AGENTS.md §5 Experience Externalization Discipline** の 2 段になる。 本 session は自己言及的に「経験値外部化 discipline を演習しながら永続化した」 session で、 反省会で気付いた pattern を即座に CLAUDE.md / AGENTS.md に encode する loop 自体が discipline の自己適用例。 詳細は `2026-05-21.md` Session 2 |
| 2026-05-21 (Session 1) | **Brief 8 / CSCI-44 (`semantic-ci target-catalog` Authoring meta surface) landed (PR #86)**。 ABCD-B 軸 4/4 完走 = **Brief 8 完走**。 `authoring/catalog.py` で 4 registry (`path_schema` / `type_schema` / `match_schema` / `TEMPLATE_CONSTRAINTS` / `Operator`) を再 import して mirror、 catalog 側で値の二重定義ゼロ (INV-5 構造的担保)。 出力は machine-readable JSON (`schema_version="catalog-1"`、 48 targets / 4 templates / 20 operators) + human-readable 4 セクション (primary_kinds / targets / templates / operators)。 `--kind` (templates narrow) / `--target-path` (targets narrow) filter は independent、 `--target-path` 未知値で did-you-mean。 **INV-5 cross-test 5 件** (key sets parity / match_schema dump parity / template constraint dump parity / operator class parity / evidence_emits AST 抽出 parity) + INV-1 (`target-catalog` 実行が verdict bytes 不変) + INV-2 / INV-4 (CSCI_44_MODULES enumeration を `tests/architecture/test_surface_isolation.py` に追加) + determinism (3 回呼び + PYTHONHASHSEED 違い) + schema valid round-trip。 evidence_emits は **`evaluator/operators.py` の AST 走査で `_evidence(**{...})` kwargs から抽出** (registry を hard-code せず producer 出力 shape を grep する CSCI-43/42 教訓 #4 を直接実装)。 dogfood: TC1 = refactor template の constraint set が catalog 出力から再構成可能であることを cross-test 1 件で固定。 **CI 3/3 green (3.11 / 3.12 / 3.13)、 1261 passed (1238 baseline + 23 new、 0 regression)**、 ruff check / format 両 pass。 PR review で抽出された P2 3 件 (本 STATUS.md `## Phase` 重複 / `次の発行順序` 未更新 / 本 `_index.md` 行未追加) は follow-up commit で解消。 設計判断のハイライト: (1) catalog builder が registry を hard-code せず再 import で mirror、 enum 追加が即 fail で drift 検出、 (2) operator evidence_emits を `_set_outcome` / `_baseline_set_outcome` / `_boolean_outcome` / `_unknown_type_mismatch` の AST 走査で抽出 (`Class::method` 受理忘れ系の trap を構造的回避)、 (3) Open Q1 catalog default = full catalog で確定 (`--kind` / `--target-path` は明示 opt-in)、 (4) INV-2 prefix match `("semantic_ci_code.authoring", ...)` で `authoring.catalog` も追加 enumeration なしで自動 cover、 (5) PR #85 で確立した「producer spec の単一 source of truth」 contract が catalog 内でも継承 (`authoring/canonical.py` 3 predicate を内部で reuse)。 修正・訂正: STATUS.md `## Phase` 既存段落を削除せず新段落を上に追加して duplicate paragraph 状態 / `次の発行順序` 未更新 / `_index.md` 行未追加 (本 follow-up で解消)。 ABCD-A + ABCD-B 完走、 残るは ABCD-C (Brief 7 SSP) + ABCD-D (P2 残課題)。 Brief 8 完走 = product 機能の中身 fix phase 完了、 残り C + D で **「ゲームとしては完成 (クローズドβ相当)」 状態**到達見込み。 次セッションは Brief 7 (CSCI-36) Task Brief 起草 (Codex 起動可能日)、 起草時必読: `docs/brief_7_planning.md §11` checklist + `AGENTS.md` Forward Design Note: Brief 7 / SSP v0.1。 詳細は `2026-05-21.md` (本 follow-up で新設) |
| 2026-05-19 | **Brief 8 / canonical-form refactor (PR #85) merged + CSCI-44 brief 起草 paste 待ち**。 開始時は CSCI-44 (`target-catalog`、 Brief 8 完走の最後の 1 ピース) brief 起草 → Codex paste の予定だったが Codex 不在判明、 AskUserQuestion 4 択 (A 別 Claude 委譲 / B 私が CSCI-44 実装 / C-1 canonical refactor / C-2 A 軸 follow-up) で user 「C-1」 確定。 5/15 Session 4 の例外運用を 2 連続にしない判断 + canonical refactor が CSCI-44 catalog builder の前提を整える依存関係。 実装: `authoring/canonical.py` 新設 (3 helper = `is_canonical_fqn` / `is_canonical_fqn_prefix` / `is_canonical_test_id`、 producer-side spec を module docstring に逐語 pin) + `pr_body.py` の private 関数 2 つ削除 + canonical 経由化 + `init_command.py` の 3 validator を canonical 経由化 + `_validate_fqn_prefix_values` inline 判定 extract + `tests/authoring/test_canonical.py` 48 parametrize cases (CSCI-42 review trail で表面化した near-miss shape 群を直接 encode)。 **dogfood**: `init --recipe refactor:preserve-api-with-allowlist` を 2 ケース実走 — (a) allowlist 無し → E_VIOLATION + 3 added public symbols 期待通り、 (b) `--allow-fqn-prefix authoring.canonical.` 付与 → verdict=pass 4/4 satisfied = §23.1 自己検証成立。 PR #85 は **CI 3/3 green、 0 review round で clean merge** (5/15 Session 4 の 13 round と対極、 brief 起草前の §15.1 Schema grounding 実走 + producer 出力 shape contract test 化が効いた)。 1238 passed (1190 baseline + 48 new、 0 regression、 1 pre-existing env failure は無関係)。 **設計判断**: (1) AskUserQuestion 4 択で「規模 × 例外運用 × 依存関係」 軸を明示、 (2) canonical module 置き場所 = `authoring/` package 直下に公開化 (private import smell 解消)、 (3) 3 helper 揃えで「producer spec の単一 source of truth」 contract 成立、 (4) architecture invariant prefix match (`AUTHORING_FORBIDDEN_FOR_VERDICT_PATH` 末尾 `+ "."`) が新 module を追加 enumeration なしで cover、 (5) CSCI-42 13 round の Codex 教訓 (near-miss shape 群) を test 化して将来 drift 検出。 **修正・訂正**: uv tool venv 内に dev deps 不足 (`uv tool install --reinstall --with ...` で解決)、 CI 環境の `commit.gpgsign true` で test commit が remote signing 400 fail → global config 変更禁止に従い `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false pytest` で per-invocation override に narrow、 `test_schemas_roundtrip` の subprocess PYTHONPATH 起因 fail は `git stash` で pre-existing 確認。 ABCD-B 軸 3/4 + canonical refactor follow-up landed、 残るは CSCI-44 のみで Brief 8 完走 → ABCD 全完走。 次セッションは CSCI-44 paste (Codex 起動可能日)。 詳細は `2026-05-19.md` |
| 2026-05-15 (Session 4) | **Brief 8 / CSCI-42 (`semantic-ci init --recipe --from-*`) landed (PR #84)**。 同日 4 セッション目、 Codex 不在のため例外措置で Claude が brief 起草 → 実装 → Codex bot review 対応 → merge を 1 session 内で全部担当。 初実装 21 files / +2853 lines (CLI 4 recipe = `feature:add-api` / `bugfix:regression-test` / `refactor:preserve-api-with-allowlist` / `test-update:add-test-case` + 4 source surface = PR body / issue / labels / commits + C1〜C4 conflict detection + 3 architecture invariant test INV-1 / INV-3 / INV-4 + provenance metadata). **Codex bot review 13 round 連続 P2 を順次消化**: strong-layer cutoff per-field → layer-wide / Python 3.12 import path + `tests/__init__.py` 不在の collection error / `--add-api` FQN validation 不在 / bloat trim (-420 lines) / bare `--allow-fqn-prefix` reject / `--test-case` refactor recipe compat / class-based pytest ID `Class::method` 受理 / over-qualified node ID reject / non-pytest function/class name reject / non-POSIX path reject / doubled trailing dot reject / non-normalized path reject + ATX heading reset。 **10 round 目で AskUserQuestion 3 択提示** (打ち止め / 漸進 / 根本 refactor) → user 「根本 refactor」 選択で `authoring/canonical.py` 集約に着手したが途中で user stop 指示、 `pull` で clean state に sync して中断 (canonical refactor は次セッション持ち越し)。 **設計判断のハイライト**: (1) Codex 不在時の design / implementation split 例外を commit message に明記 + 次セッション復帰 pin、 (2) trim commit `b0c5855` で bloat を -420 lines (`module / class / function docstring の自明なもの全削除` + 単一 site で呼ばれる helper 全 inline + `_is_fqn` 重複定義を pr_body 単一 source 化)、 (3) AskUserQuestion で trade-off 軸を明示する pattern が単純な「続けるか?」 より判断早い、 (4) extractor actual output shape を **grep してから** validation を書く必要性 (`Class::method` を「emit されない」 と思い込み reject → P2 で `python_test_surface_extractor.py:190` を指摘されて反転)、 (5) Codex 13 round の root cause = validator 重複 + extractor 出力 shape 暗黙追従、 canonical-form module 1 集約で根を絶てる (実装は次セッション)。 **修正・訂正**: CI 3.12 collection error (`tests/__init__.py` 不在で `from tests.cli.helpers` が 3.12 stricter resolution で fail) / `ruff format --check` 抜け (local は `ruff check .` のみで CI は 2 step) / `_is_test_id` 過剰 strict (`count("::")==1` で class-based reject、 反転) / `_is_valid_fqn_prefix` の `rstrip(".")` で `pkg..` 受理 → `value[:-1]` で exactly 1 dot strip。 ABCD-B 軸 3/4 landed、 残るは CSCI-44 のみで Brief 8 完走。 1191 passed (3.11 / 3.12 / 3.13 全 green)。 詳細は `2026-05-15.md` Session 4 |
| 2026-05-15 (Session 3) | **Brief 8 / CSCI-43 (`semantic-ci target-doctor` Advisor surface) landed (PR #82)**。 同日 Session 2 (CSCI-41 docs only) を実装に展開、 推奨着地順 41 → **43** → 42 → 44 の 2 番目消化。 `cli/commands/target_doctor.py` + `authoring/hazards.py` + `authoring/advisory.py` + `cli/output/doctor_human.py` / `doctor_json.py` 新設、 `tests/architecture/test_surface_isolation.py` で INV-2 (verdict-bearing module = `check` / `compare` / `pre_commit` + engine の transitive imports に doctor module が混入しない) + INV-4 (CLI dispatcher 例外を `main.py` に narrow) を gate、 schemas/doctor_advisory.schema.json で envelope を `schema_version="advisory-1"` 固定、 exit code §6.3.3 規約 (advisory ≥ 0 でも 0、 入力 2 / engine 3 / unhandled 4)。 **Codex bot review 16 round 全部 P2 消化** (本体 1 commit + 16 fix commit、 merge `66b6fc2`): R1 = SKIPPED-by-design operator 経由 D4 / R2-R3 = `not_equals(non-empty)` / zero-magnitude を P1/P2 で握り潰し / R4-R6 = `equals_baseline` / `subset_of` / rename `old_path` の D4 取りこぼし / R7 = `severity: info` constraint を D4 lock-only から除外 / R8 = dict-nested expected の意味的等価重複 D3 / R9 = `unknown_policy in {fail, repair}` の S1 / D4 評価不一致 / R10-R12 = zero-shape / partial-dict / delta observation 軸の lock-only narrow / R13-R14 = `--package-root` 配下外 path / open path 扱い / R15-R16 = leaf target + numstat scope。 **R17 (`--package-root .` cwd vs `check` repo-root divergence) は post-merge 遅延配信、 deferred follow-up** (critical でない UX consistency 改善)。 **設計判断のハイライト**: (1) INV-2 architecture test を実装より先に書く pattern が効いた (Session 2 メモ「surface boundary を docs に書く前に既存実装を inventory」 を直接継承)、 (2) 「lock-only と vacuous-pass」 の境界が D4 false-negative の主因で R10-R12 の 3 重 narrow が必要、 §6.3.1 の advisory spec が inviolate predicate 形式で書かれていなかった反省、 (3) 16 round 全部 P2 = spec vs 実装の boundary 曖昧、 CSCI-42 / 44 brief では「advisory / generator 1 つごとに inviolate predicate 1 行」 + 「false negative 境界 fixture を AC 要求」 の pattern を継承。 ABCD-B 軸 2/4 landed、 次は **CSCI-42** (`init --recipe --from-*`)。 1072 passed (+66 new test、 3.11 / 3.12 / 3.13 全 green)。 詳細は `2026-05-15.md` Session 3 |
| 2026-05-15 (Session 2) | **Brief 8 / CSCI-41 (Authoring Surface 設計契約) landed (PR #81)**。 同日 Session 1 (ResultStatus split 4 PR) 完走後の docs only セッションで B 軸 (Brief 8) 入口を 1 PR 4 commit で消化。 `docs/target_authoring_surface.md` 新設 (6 点 A-F: hand-written 非必須 / 生成経路 3 通り / LLM 経路 Brief 8b 分離 / verdict 前 freeze / surface isolation 但し Advisor renderer exempt / `candidate_code_used: false` 固定) + 連動 cross-ref 7 file (design.md §23.3.1 + §24 + §25 / target_yaml_guide.md / cli_usage.md / exit_codes.md / json_schema.md / CLAUDE.md / README.md)。 **Codex 3 round 全部 P2 消化**: Round 1 = cli_usage.md scope creep (4 subcommand bullet 先取りで parser drift) → trim commit `4167f3b` で AC 「のみ」 制約に narrow / Round 2 = Section F 内部矛盾 (`every Brief 8 path records candidate_code_used: false` が plain `init` の TARGET_TEMPLATE byte-invariant と衝突) → narrow commit `2995f7c` で `generation_metadata` populate を generator paths 限定 / Round 3 = Section E import isolation contradiction (`compile-repair` を verdict-bearing 側に分類してしまい `repair_compiler/adapters/format_generation_metadata` 既存仕様と衝突) → exemption commit `8b24249` で Advisor renderer を INV-2 適用外と明記。 **設計判断のハイライト**: (1) `brief_8_planning.md` の docs table 追加見送り (user 指示「不必要ならテキストをいろんなところに置きたくない」 に従い STATUS.md でカバー)、 (2) AC 「のみ」 制約は逐語遵守が Codex review round 数を減らす経験則 (セルフレビュー + Codex の同一 scope creep 指摘で再確認)、 (3) Codex 3 round の本質 = surface boundary を doc に書くなら既存実装の boundary 仕様 (`format_generation_metadata` / TARGET_TEMPLATE 逐語維持) と矛盾しないか implementer 視点で audit 必要、 「verdict-bearing vs Advisor renderer」 の inviolate boundary に立ち戻ると 3 round 全部が一貫した narrowing に収束 (PR #78 の同日 ResultStatus 3 round と同 pattern)。 ABCD-B 軸 1/4 landed、 次は **CSCI-43** (`target-doctor`、 1.5 日、 INV-2 / INV-4 早期固定)。 1006 passed (3.11 / 3.12 / 3.13 全 green)。 詳細は `2026-05-15.md` Session 2 |
| 2026-05-15 | **ResultStatus split 完走 (4 PR 一気通貫: D1-2 / D1-3 / D1-4 / D3)**。 1 セッション内で planning §3 D1〜D3 の全本体 brief を main landed。 PR #76 (D1-2 / `compiler/operator_schema.py`、 (kind, operator-class, path-domain) を `CompileError` reject、 994 passed) → PR #77 (D1-3 / `compiler/type_schema.py`、 `TargetCategory` 9 値 + Pydantic 反射 path→category map、 observed-category + expected-shape の 2 軸検証、 975 passed、 CI で `ruff format --check` fail を follow-up commit で消化) → PR #78 (D1-4 / `UnknownCause` StrEnum 4 値、 `ConstraintResult.unknown_cause` + `_aggregate` で AUTHORING 強制 FAIL、 全 formatter に surface、 nested optional bump 例外規定を `docs/json_schema.md` に新設、 1006 passed、 **Codex P2 review 3 round 消化** = open path retag / 3-way `_resolved_unknown_cause` / D1-3 observed-vs-expected split) → PR #79 (D3 / `risk_summary.authoring_errors` を `would_violate` から分離 + `RISK_SUMMARY_KEYS` 先頭に配置、 single-evaluation refactor、 3 adapter に 2-step implementation order intro、 validate-plan envelope `"1"→"2"` bump、 6 golden fixture refresh、 1006 passed)。 **設計判断のハイライト**: (1) `UnknownCause` を `evaluator.evaluator` に enum 定義 + operators.py からは string で渡し境界で enum 変換 (循環回避)、 (2) D4 envelope bump policy の 2 ケースを `docs/json_schema.md` に明文化 (nested optional 例外 vs depth-1 top-level)、 (3) Codex 3 round は全部 P2 design soundness で「authoring vs runtime cause の境界が曖昧」 という設計問題 — 「authoring = compile が押し戻せなかった spec malformed」 という inviolate definition に立ち戻ると 3 round 全部が一貫した実装に収束。 ABCD-A 軸 landed、 残り B (Brief 8) / C (Brief 7 SSP) / D (P2 残課題)。 次セッションは CSCI-41 (Brief 8 docs only、 半日) を fresh context で着手予定。 詳細は `2026-05-15.md` |
| 2026-05-12 | **ResultStatus planning 取り込み (PR #74) + ABCD 完成度境界の確認**。5/9 branch 残置の commit `19e47bd` (`docs/brief_resultstatus_planning.md`) を main 取り込み。 取り込み際の audit で **Brief 8 (Authoring Surface)** が 5/9 以降 landed していること発見、 working title 衝突 + 設計射程 overlap を発見し **§1b "Boundary with Brief 8" 4 節**を新設してから取り込み: §1b.1 error class boundary 表 (semantic hazard = Advisor / syntactic-type = Validator) / §1b.2 INV-1 framing (D2 は malformed-input domain 縮小、 INV-1 は well-formed input domain 要求) / §1b.3 ADVISORY-S1 文言更新 follow-up を D1-4 PR で call out / §1b.4 着地順序 open question。 main 直 push は server 403 で denied、 user 「PR 可」 で経路切替 → PR #74 として merged (`9679a9b`)。 後半で **ロードマップ全体確認 + 完成度の率直な見積もり** に展開、 ABCD 完成度議論 2 巡 (1 巡目: 外部 readiness gap 11 項目で OSS ~50% / 2 巡目: user 問い「product 機能としては?」 で軸分離 → 「ship-blocking 機能 gap 無し / scope choice / improvement の 3 分類」 で再 framing)。 **半年強の壁打ちが ABCD という形に蒸留された節目**を言語化、 「中身が固まる = post-ABCD の future state、 重心移動 = internal 凍結ではなく external signal 駆動への切替」 を precise 化。 次セッション以降は ABCD implementation phase 開始、 直近最短経路は **D1-2 paste** (brief 起草済、 `2026-05-09.md` 参照) or **CSCI-41 起草** (Brief 8 docs only)。 詳細は `2026-05-12.md` |
| 2026-05-09 | **緊急 perf brief 2 連続 + ResultStatus split planning + Brief D1-2 起草**(PR #70 D2-1 / PR #71 D2-2 + planning doc 起草)。D2-1: `tests/cli/helpers.py::run_semantic_ci` を default で in-process `cli.main()` に切替、 wallclock 98s → 24.5s(Linux ローカル -75%、 CI 1 job 180s → 90s)、 subprocess は PYTHONHASHSEED determinism / console-script entrypoint / cache-hit sentinels / smoke benchmark のみ残置、 `@pytest.mark.slow` で wallclock 比較 test を default 除外。D2-2: `tests/cli/conftest.py` で session-scoped template repo 3 種を 1 度 build、 各 test は `clone_template_repo` で `shutil.copytree` (Windows 優先) / `git clone --local --no-hardlinks` (POSIX) で複製、 verdict-shape 6 件を `check` → `compare` 経由に migration、 Windows 264.92s → 181.61s (-31.4%) で <150s 未達も実用閾値クリア。Claude review で指摘した determinism test の smoke + no-fetch 取り消し / missing-package-root の check 経路復元は merge 前に fixup `7a20f20` で消化、 follow-up 不要。間に user 直列対話で ResultStatus split planning を **C+B 仮固定**(`unknown_cause` sibling field / authoring を compile-time に押し戻し / 5 brief 分割 D1-1〜D1-4 + D3)で確定、 `docs/brief_resultstatus_planning.md` を起草(branch `claude/daily-tasks-cbQj7` の commit `19e47bd` 残置、 main 未反映)。Brief D1-2 (`E_OPERATOR_TARGET_MISMATCH` を compile-time 化) Task Brief を起草、 user → Codex paste 待ち。1 セッションで 3 PR merge + 2 brief 起草 + planning 1 本完成は最高密度。詳細は `2026-05-09.md` |
| 2026-05-08 | **PR #69 (`docs/target_yaml_guide.md` 新設) + ResultStatus 棚卸し start**。docs only セッションで `docs/target_yaml_guide.md` を新規作成、 2026-05-07 Session 4 dogfood で抽出された D1/D3/D4 (`--package-root` scope 制約 / template と user constraint の重複 / config-only PR の vacuous PASS) を authoring hazard 章として集約。 CLAUDE.md docs table + design.md §24 + README に追従、 §23.3 boundary を冒頭 reminder に pin。 Codex bot review 3 round (P2 P2 P2) を消化(`primary_kind` 表が auto-installs を実装と乖離して書いていた / D4 overstate / §23.3 例の `os.system` が effect-template-gated)、 user merge。後半で `次の発行順序 §E (ResultStatus 概念モデル更新)` の first task = call-site 棚卸しに着手、 UNKNOWN emit ~24 site 分類 catalog を branch 残置(commit `6a07aee`)。詳細は `2026-05-08.md` |
| 2026-05-02 | P1 effect extractor 3-stage 完成 (CSCI-2/3/4): direct-call + imported-alias + global_mutation。PR #3/#4/#5 merged。`importlib.resources` 化 / TryStar 対応 / compound 文 module-scope 走査などの self-review fix を含む |
| 2026-05-03 | design.md に §17-§22 追加 (Spec Authorship / Performance Budget / Spec Quality / Suite Packaging / Vibe Coding Adapter / Multi-language Phasing) + §17/§18 を §23/§24 へリナンバー + Brief 5-7 追加。AI 時代の vibe coding 要件に対応するため Generator Adapter / Repair Compiler を P5 → P2.5 へ前倒し、TS を P3b → P2.5 並列へ前倒し |
| 2026-05-03 (Session 2) | Brief 2 完結: Python P1 抽出器 5 PR (CSCI-5〜9 / api_surface / imports / module_graph / complexity / test_surface) を 1 セッションで連続 merge。全抽出器を stdlib `ast` のみで実装 (griffe/radon/lizard/pytest --collect-only 不採用)、subprocess 跨ぎ determinism テスト共通化。CSCI-8 で Brief 内算術タイポを Codex が escalate して修正。`CodeState` 6 次元すべて populatable に。次は Brief 3 (pipeline) で CSCI-10〜14 への分割を推奨 |
| 2026-05-03 (Session 3) | Brief 3 (判定層) planning を Q1〜Q4 で確定: B (バランス) / 5 PR 細分割 / B (緩める呪文 P1 不採用) / B' (B + stable error code)。設計上の根本的気付き「詳しく書けすぎると逆にパスしてしまう」「目的と現コードがあれば仮想コードでも判定できる = generic 2-state comparator」を言語化、design.md §23 (Comparator Architecture and Application Matrix) を新設して §23/§24 を §24/§25 にリナンバー。`docs/brief_3_planning.md` 作成 (Engine API 契約 + Brief 4 申し送り含む)。`CLAUDE.md` に Session Memory ワークフローを正式追加。次は CSCI-10 Task Brief 発行から |
| 2026-05-04 | Brief 3 残務 + Brief 4 全体 (CSCI-15〜19) を 1 セッションで完結。`docs/brief_4_planning.md` 作成 (PR #20)、CSCI-15 (#21) / CSCI-16 (#22) / CSCI-17 (#23) / CSCI-17b (#24) / CSCI-18 (#25) / CSCI-19 (#26) を順次 merge、`semantic-ci` CLI 5 subcommand release 可能状態に。CSCI-16 で **self-dogfooding が `json_basic.py` 削除リグレッションを検出** (semantic-ci がツール本体の不変条件 enforcer として機能した最初の瞬間)。CSCI-17 から brief 圧縮版 (Q&A 表 + AC 1 行 + 250 行目安) に切替、品質劣化なし。`docs/cli_usage.md` / `exit_codes.md` / `json_schema.md` 整備。Open Questions 棚卸しは Brief 5 起草前に必要。次は Brief 5 (Vibe Coding Adapter / Repair Compiler) の planning から |
| 2026-05-05 | Yahoo 記事から始まり OpenMythos 解析 → semantic-ci で audit → 3 段階の実験(`vfr_vs_ifr` / `run_matrix` / `run_pregen`)で **§23.1 入力 contract「engine は state の出自を問わない」を仕様から検証済み性質に格上げ**。PR #34 で `docs/pre_generation_validation_case.md` + `experiments/pre_generation_validation/` 新設(top-level `experiments/` ディレクトリ初登場)。bot review (reproducibility 指摘) にトーンダウンではなく **self-contained 化** で対応 — 合成 baseline `tinypkg` (25 行) で外部依存ゼロの reproduction を実現。VFR/IFR / コンパス / 計器飛行の比喩が 5 ケース matrix で 5/5、最大 20× S/N 圧縮の数値として裏付け。intent と raw 差分の **直交性** が定量的に確認された。次は Tier 1 残課題(Repair loop closure / Effect-dimension drift / Allow-list 政策)+ Brief 5 planning |
| 2026-05-05 (Session 2) | PR #34 マージ後の post-merge 検証で reproduction を `/tmp/` 隔離で完走 → 実コード baseline (`src/semantic_ci_code/`) 版で 3/3 一致 → ユーザのマッチポンプ批判を受けた **adversarial test (A 真陰性 + D 偽陰性)** に拡張。A pass で「engine = fail-everything」の強い形は反証、D_real_effectful の MISS から **effects slice 実装 gap** を発見: extractor の `fqn` が callee 指向 (設計 §3.1 は enclosing function を要求)、`Path.write_text` 未検出。`docs/code_semantic_ci_design.md` §3.1 / §12 P2 と突合し **設計射程内・P2 で予定通り解消される話** と確認。実装は (1)分業ルール (2)ブランチ衛生 (3)テスト影響評価 を理由に持ち越し。Task Brief 候補 (Fix 1: extractor `fqn` semantics 修正、半日規模、P1 hot-fix) として整理。repo は無変更、検証スクリプトは全て `/tmp/real_code_validation/` に隔離。次は Brief 化判断 + adversarial test の `experiments/` 化検討 |
| 2026-05-05 (Session 3) | Brief 4b cache / 性能 slice を CSCI-25 / 26 / 27 の 3 brief 連続発行で完結。CSCI-25 (#33): `--mode {smoke,full}` (smoke = api_surface + imports + effects 3 次元、 partial state は evaluator で SKIPPED routing、 schema v1→v2 bump で `mode` + `summary.skipped` 追加)。CSCI-26 (#37 + follow-ups): `check` の CodeState cache、 Option A (CodeState cache) を Option B (worktree) より採用、 cache key core は `git rev-parse <commit>:<package_root>` の subtree object id、 corrupt は miss 扱い、 schema bump なし。CSCI-27 (本セッション merge): pre-commit cache (`git write-tree` で staged を SHA 化) + JSON cache stats (schema v2→v3) + size-based eviction (LRU by mtime, 100 MiB default) を 1 brief に同梱、 cache 系を leave 可能状態に。本体は Brief 5 (Vibe Coding Adapter) に戻る判断、 cache 残作業 14 項目は CSCI-28+ で Brief 5 後に再評価。brief 圧縮テンプレ (Q&A 表 8 セクション固定) を 3 連続実証 |
| 2026-05-05 (Session 4) | adversarial test (A 真陰性 + D 偽陰性) で effects slice 実装 gap 発見 → Brief 3/4 残課題の **再分配**(Brief 4b/4c/4d 並列 + Brief 5/6 並列 + P2 細目化)を docs に永続化、**P1 完走**(Brief 4b/4c/4d 全 merged)→ **Brief 5 (P2.5 entry) planning** まで 1 セッションで一気通貫。merged: PR #35 (status visibility) / PR #36 (redistribution) / PR #39 (Brief 4b planning) / PR #40 CSCI-28 (SARIF + GH Actions + pre-commit manifest) / PR #42 CSCI-29 (effects fqn fix) / PR #43 CSCI-30 (init + authorship + severity routing) / PR #44 (Brief 5 planning、Codex bot review fix 込み)。Brief 4d は planning doc なしで Task Brief 直接出力 pattern を確立、brief size に応じて pattern 可変化。redistribution table (§25) で「Brief 7+ deferred 押し込み」 を「行先明示」 に変換、忘却ゾーンを消した。次は Brief 6 planning(TypeScript、Brief 5 と並列発行可)+ CSCI-31 Task Brief から |
| 2026-05-06 | Responsibility Boundary を engine contract として確立。 user の「target.yaml 取り扱いがぶれて見える」 観測 → 7 件具体問題抽出 → user 原則言明「declared intent から決定論的、 瑕疵・逸脱・真意は判定外、 補正誘導・入力補足は検討価値あり」 → §23.3 を §23.1 (state 側) の **intent 側鏡像**として新設、 4 surface 模型 (Validator / Authoring / Provenance / Advisor) で既存機能を分類、 CLAUDE.md Scope guard を「This is not」 denial 形で 2 行拡張 (`not an intent validator` + `not an intent interpreter`)。 commit `fa5f887` 8 ファイル 1 PR で merged。 既存 §23.3/§23.4 を §23.4/§23.5 にリナンバ + cross-ref 2 ファイル追従。 brief_5_planning.md §4.1.1 で Adapter render-not-translate invariant を要件化。 Codex 不在で brief 発行なし、 docs only 軽量セッション pattern が成立。 次は A (code 側追従 small brief: evaluator INFO 明示分岐 + init template コメント + authorship 不可侵 invariant test) → B (CSCI-31 Brief 5 entry) → C (Brief 6 TS planning) |
| 2026-05-06 (Session 2) | Issue #48 (Semgrep を core に深く統合する提案) を Brief 化前 audit。 Semgrep 1.161.0 で決定論性 8 項目を実機検証、 5 重大発見 (OSS fingerprint 不在 / line-based fp 不安定 / path & rule_id 正規化必須 / paths.scanned 突合必須 / airgap flag 群固定) で当初前提崩壊 → core 統合 reject、 別 protocol **SSP (Semantic Security Protocol)** として §20.1 layered distribution の 4 層目に分離する設計に転換 (PR #50 で `design.md §20.1` 自体を 3 層 → 4 層に更新済み、 `semantic-ci-ssp` を suite と並列追加)。 6 論点を直列確定: Q1=B (SAST+SCA) / Q2=A (Python only) / Q3=B (**5 要素 fp** = `rule_id × module_path × qualified_name × normalized_text × ordinal`、 canonical JSON injective encoding、 ordinal は 4-tuple group → source-span 昇順 sort → 0-indexed で pin) / Q4=B+仮想コード対応 (Sensor Provenance Invariant、 §23.1 の SSP 鏡像、 4 mode = real/staged/virtual/contract-test) / Q5=A (#48 close + 新規 tracking) / Q6=A (SSP のまま、 NIST SSP 衝突は許容)。 Q3 fp 算法は本セッション当初は 4 要素 + delimiter 連結だったが、 PR #50 review で **3 段階に refine** された (詳細は `docs/brief_7_planning.md §4.3` 設計改訂履歴 callout)。 Q2 詰め中に user が **Brief 6 (TypeScript) 凍結** を表明、 SSP 議論と一体で確定 → 順序は **Brief 5 → Brief 7** に再編。 PR #49 (元は Brief 4b/4c/4d status refresh の小 PR) は merge 済み、 PR #50 (本 PR、 SSP planning + Brief 6 凍結 + 上記 fp refine + §20.1 4 層化) は review 中: `docs/brief_7_planning.md` 新設 (12 節、 6 論点確定 + audit invariants + CSCI-36〜40 候補 + 次セッション着手 checklist) + design.md §12 P3b 凍結注記 + §20.1 4 層化 + §25 表更新 (Brief 6 打ち消し / Brief 7 行追加 / Brief 7+ deferred → Brief 8+ rename) + CLAUDE.md Current Status (Brief 5 → 7 順序 + Brief 6 凍結) + docs table (brief_7_planning.md 行、 5 要素 fp + provenance 注記)。 audit fixtures `/tmp/sci_audit/` は本セッションで永続化せず Brief 7 着手時の判断に持ち越し。 **Issue #48 はクローズ済み** (state_reason=`completed`、 SSP 文脈移動 redirect コメント付き、 #48 → PR #50 で track)。 「Codex を起動できない日でも進められる docs / planning セッション」 が 2 連続で成立。 次は (1) PR #50 merge、 (2) Brief 5 (CSCI-31) 発行、 (3) Brief 5 完了後に Brief 7 (CSCI-36) 発行、 (4) **Brief 7 着手時の必読**: `docs/brief_7_planning.md §4.3` 設計改訂履歴 callout で fp 算法の最終形を確認 |
| 2026-05-06 (Session 3) | PR #50 (Session 2 で起票した SSP planning + Brief 6 凍結) を Codex bot review が 7 round 連続で指摘 → 順次対応 → merge までを 1 セッションで完結。 design decisions の本質 (SSP 存在意義 / Q1-Q6 決定) は不変、 spec の **implementability** (CSCI-36/38 implementer が exact behavior を再現可能な level の specificity) / **forward-doc consistency** (planning doc と repo state を同 PR boundary で同期) / **memory-as-handoff** (memory が次セッション handoff の source of truth) の三軸で refine。 Round 1 (P2): Brief 6 凍結を forward 全 docs に sweep (`brief_5/4b/4_planning.md` + `design.md §25` callout)、 commit `5469748`。 Round 2 (P2): SAST fp 4→5 要素拡張 (`module_path` 追加、 同 rule × 同 normalized_text × 別ファイルの衝突源除去)、 commit `e17febc`。 Round 3 (P2+P3): SHA-256 入力を delimiter 連結 → **canonical JSON 配列 (injective encoding)**、 `:` 含む normalized_text の非単射衝突を除去 + `design.md §20.1` 自体を 3 層 → 4 層に実装 (`semantic-ci-ssp` を suite と並列追加)、 commit `45f47a6`。 Round 4 (P2): `Brief 7+ deferred` → `Brief 8+ deferred` rename を §25 表だけでなく forward-pointing 全箇所 (LSP / 追加 adapter 等) に sweep、 commit `dde8e74`。 Round 5 (P2): **ordinal 規則 pin** = 4-tuple group → source-span tuple `(start_line, start_col, end_line, end_col)` 昇順 sort → 0-indexed (重複 span は ordinal 割当て前に dedup、 virtual mode は sensor provenance の stable iteration order)、 commit `e3c94a0`。 Round 6 (P2): `_index.md` を「historical log」 ではなく「handoff source of truth」 と再定義、 Session 2 行を post-review 状態に full rewrite + `2026-05-06.md` Session 2 narrative 冒頭に**改訂注記 block quote** 追加 (narrative 本文は historical record として不変)、 commit `8b46dd2`。 Round 7 (P2): `§4.5` Q5 を「現状記述」 に書換 (#48 closed、 SSP tracking は PR #50 + planning doc 自体、 新 tracking issue 起票は **Brief 7 着手時に pin**) + §11 着手 checklist に**起票テンプレ** (title / body / sub-task list) 追加、 commit `fff79a9`。 PR #50 main へ squash merged (`099c617`、 8 commits / +541/-17 / 8 files)。 設計改訂履歴 3 round 分は `brief_7_planning.md §4.3` 末尾の callout に集約済み (後 implementer が git archeology せず到達可能)。 **教訓**: spec doc の draft が「decision level」 と「spec level」 の中間にあると review が長引く、 「決定論的算法を凍結する」 と宣言する以上 implementer 再現可能性を self-audit すべき / `_index.md` は handoff source of truth として operational、 historical log との dual responsibility を持つ / forward 文書と memory の責務分離は同 PR 内で更新粒度を変える根拠 / planning doc に「設計改訂履歴 callout」 を置く pattern は spec design 一般に転用可能。 「Codex 不在で進める docs / planning セッション」 が 3 連続で成立 (5/6 Session 1 = §23.3、 Session 2 = SSP design、 Session 3 = SSP implementability)。 次は (1) Brief 5 (CSCI-31) 発行、 Codex 起動可能日が前提、 (2) Brief 5 中の Brief 7 阻害回避は `brief_7_planning.md §8` の 4 条件を毎 PR 確認、 (3) Brief 7 (CSCI-36) 着手時は **§11 checklist 順序通り** (新 tracking issue 起票 → CSCI-36 Task Brief 発行 → audit fixtures 永続化検討)、 起票テンプレは §11 に full pin 済み |
| 2026-05-07 | **Brief 5 (P2.5) 完走 / PR #57 で完了宣言 landed** — CSCI-32〜35 の 4 PR を 1 セッション内で順次 review → user merge → 次 brief 発行の循環で消化。merged: PR #53 CSCI-32 (Cursor `.mdc` adapter) / PR #54 CSCI-33 (Codex plain-text adapter) / PR #55 CSCI-34 (`compile-repair` subcommand + `RepairPlan` JSON deserializer + verdict envelope auto-detect + 独立 envelope `schema_version="1"`) / PR #56 CSCI-35 (`validate-plan` subcommand + `risk_summary` 4 要素計算 = `would_violate` / `forbidden_zones` / `required_additions` / `template_implications` + 独立 envelope `schema_version="1"`)。`semantic-ci` CLI は 8 subcommand (init / observe / compare / check / pre-commit / compile / compile-repair / validate-plan)、Vibe Coding Adapter 3 種(Claude Code / Cursor / Codex)release 可能状態。**Brief 制約の事後撤回 pattern を初実行**: CSCI-35 brief で「Adapter Protocol signature 変更禁止」 と書いた制約に Codex が忠実に応答し ContextVar 経由で risk_summary を渡す悪手実装を選択 → Claude review で brief 制約自体が誤りと判定し撤回 → fix commit `b87c2c0` で `render_pre_gen(target, baseline_state, *, risk_summary=...)` 明示引数版に切替、ContextVar 全廃。教訓「brief OUT は最小限、過剰制約は overengineering を誘発」。**review pipeline で sweep**: CSCI-34 review で指摘した `command_support` private import 問題を CSCI-35 brief の hint に組み込み → CSCI-35 PR 内で public alias 経由に sweep 完了 (`compile_repair.py` も連動修正)。**`would_violate` の delta-kind 盲点**を発見 (self-comparison では空 delta → delta-kind constraint は常に拾えない、planning §7.3 の意図とギャップ)、blocking ではなく CSCI-35b sweep brief 候補に組み込み。**完了宣言 commit**(本 session 末尾): CLAUDE.md Current Status を P1 完走 → P2.5 完走に更新 + brief_5_planning.md 冒頭に Status callout 追加 + §10 CSCI 表を merged 状態に + §14 残課題に CSCI-35b sweep brief 候補 4 件 (would_violate 盲点 docs / round-trip コメント / symlink 防御 / human-friendly Forbidden Zones) を pin。教訓: brief 制約は不可侵ではなく review で更新可能、Codex は制約に忠実すぎるので review 側で緩める方が早い。次は (1) **CSCI-35b sweep brief**(優先、半日規模、`brief_5_planning.md §14` にチェックリスト pin 済み)、(2) **Brief 7 (SSP v0.1) entry**(`brief_7_planning.md §11` checklist 順序通り、CSCI-36〜40 想定の数日〜1 週間規模)。CSCI-35b → Brief 7 の順が Brief 5 負債を持ち越さない |
| 2026-05-07 (Session 2) | **並走 brief 発行セッション + Brief 7 設計申し送り永続化**(Session 1 と並行に走った別 Claude セッション、本セッションの自記)。Session 1 が PR review + Brief 5 完了宣言を担当している裏で、本セッションは brief 起草側 pipeline を担当: CSCI-30b (Boundary code 追従) / CSCI-31 (Repair Compiler core + Claude Code adapter) / CSCI-32 (Cursor adapter) / CSCI-33 (Codex adapter) / CSCI-34 (`compile-repair` subcommand) の 5 brief を起草・user に提示(全 brief は Session 1 経由で merged)。CSCI-31 review の 3 round で確立された 5 軸 invariants (kind / severity / target / operator / status 逐語出現) を CSCI-32 / 33 / 34 brief に **逐語継承** → Codex 側 review round が 3 → 0 → 0 に短縮(brief invariant 継承テンプレ化が成立)。並列発行可否を「ディレクトリ分離 + import 一方向 + register 配列 1 行追加のみの共有点」 で事前判定する pattern を確立、CSCI-30b+31 / CSCI-32+33 は並列、CSCI-34+35 は `cli/main.py` subparser 衝突で順次。途中で **user から Brief 7 (SSP v0.1) 設計申し送り 11 項目到着** → 永続化先 4 択を提示し user が「`AGENTS.md` と Claude memory に直 commit、PR 不要」 を選択 → AGENTS.md `Forward Design Note: Brief 7 / SSP v0.1 (CSCI-36 着手時必読)` section を `Related Documents` の前に追加(申し送り原文逐語 11 項目 + Brief 5 学び 3 項目 + 設計 AI への推奨判断 + 文字化け verify 注記)+ 本 memory 追記、を main 直 commit で永続化(`.claude/memory/` 例外規定の AGENTS.md への **一回限り拡張**)。`docs/brief_7_planning.md` 文字化け報告は `file` cmd で valid UTF-8 確認、local 表示起因と判定、ただし planning≠spec の方針 (CSCI-36 で `docs/ssp_protocol.md` 新設) は採用。**修正・訂正**: 本セッションは Brief 5 完走を認識せず CSCI-34 brief 発行で止まっていると認識していた、push 時の `git fetch` で Session 1 が並行に CSCI-34 / 35 review + merge + 完了宣言 PR #57 まで進めていたことを発見、wrap-up を Session 2 として既存ファイルに追記する形で整合 → **並行 Claude セッション運用時は push 直前 fetch 必須**(暗黙ルール化)。次は Session 1 narrative の引き継ぎを尊重: CSCI-35b sweep brief → Brief 7 (CSCI-36) の順序維持、CSCI-36 起草時は AGENTS.md `Forward Design Note` を一次資料として逐語参照 |
| 2026-05-07 (Session 3) | **Kai 探索 + call_graph dimension 検討、いずれも採用見送りで確定**。「Kai = 走行中ナビ」 という比喩から始まり、Kai v0.14 binary install + 3 候補(C1: API rename / C2: disk write / C3: 等価 refactor) で **4 ツール比較**(semantic-ci / Kai / Claude-as-judge / Semgrep) を実走。発見: (1) semantic-ci が C2(`pathlib.Path.write_text` local import 越し)を **false negative**、(2) Kai v0.14 は **Python の cross-file ナビ未実装**(`kai analyze calls/deps` の help に "(JS/TS)" 明記、real repo 76 ファイルでも `kai_callers` / `kai_dependents` / `kai_impact` 全部 0)、(3) Kai intent は 3 候補すべて同一("Update counter in General" 38%、§23.3 の denial を empirical に validate)、(4) Claude with/without Kai context で答え同一(diff が読める範囲では Kai 冗長)、(5) **Semgrep 標準 ruleset(`p/python` / `p/security-audit` / `p/r2c-best-practices`)は C2 を拾わない**(security focus、policy violation は射程外)、custom rule のみ line 14 で検出。**170 行 PoC**(stdlib `ast` のみ)で `callers_of(extract_python_effects)` = **87 件**抽出(Kai は 0)、Kai のナビ核心は再現容易と実証。**Kai 汚染除去**(stash + .kai/ + 2 hooks `kai-managed-hook v3` + `.git/info/exclude` 1 行)を semantic-ci 検証付きで完全実施(`git diff origin/main` empty、`semantic-ci observe` 650 symbols / 192 imports / 15 effects parse OK、HEAD 不変)。Kai init が README 主張と異なり **default で pre-commit / pre-push hook を install** することも記録。**核心の発見**: 仮想 graph delta computation は repo に既にある(`_module_graph_delta` in `delta/code_state_delta.py:207`)、`/tmp/graph_test/` で target.yaml constraint 経由の動作実証(edge 追加 → fail E_VIOLATION、no-change → unknown→pass)。call_graph dimension の評価が **3 段反転**(初期不要 → 仮想コード考慮で必要寄り → module_graph 既存判明で再度不要)。**結論**: (A) Kai 統合不採用(Python 未実装 + §23.3 グレー + intrusive hook install + Python では Kai 固有の技術優位なし)、(B) call_graph dimension 当面見送り(YAGNI、`module_graph_delta` で多くカバー、C2 は別問題)。**直近優先順序**: (1) effects extractor 強化(C2 治す、CSCI-35b 同梱可)、(2) Brief 7 SSP、(3) `module_graph_delta` docs 充実(CSCI-35b 同梱可)、(4) `acyclic` operator 追加(オプション)、(5) call_graph dimension 追加は当面見送り。観測ノート `docs/kai_observation_case.md` 素材は揃ったが本セッションは commit せず(実コード / 実 docs 影響ゼロ要請)、CSCI-35b と同タイミングで起こすのが理想。教訓: **「実装しない決定を実証データで根拠付ける」 docs only セッション** pattern が成立、Brief 8 候補を 2 件却下することは planning level commit 相当の価値。**注**: 本セッション中ローカル `main` が origin から 21/126 commits 乖離していた stale state を `git checkout -B main origin/main` で正規化、これは memory 直 push の通常運用範囲内 |
| 2026-05-07 (Session 4) | **弱点分析 → 実走でクリティカルバグ発見 → 緊急パッチ 3 連続(PR #58/#59/#60)、本日 4 セッション目**。外部 review 入力「semantic-ci-code の弱点 5 点」 から 4 ラウンド(批判 → 反論 → 再反論 → 最終結論)で抽象的弱点 4 点に絞り込み(① ロードマップ非整合 / ② extractor efficacy / ③ CI 軽微負債 / ④ symlink)、**user の "君が実際叩いてみて辿ったフローか?"** で**実地ドッグフーディング**へ転換、`init` → `compile`/`validate-plan`/`compare` を end-to-end で踏んで **3 段の silent failure** を発見:(1)公式お手本(`design.md §4.5` + `tests/fixtures/compiler/design_feature.yaml`)に**フィールド名 typo × 2 + operator-target 型不整合 × 1**、(2)`compile` が path 検証していない / `validate-plan` が UNKNOWN を `would_violate` から silent drop / `check` でも did-you-mean 提案無し、(3)AI が docs を学習データに取ると壊れた pattern が伝播。これを起点に **PR #58**(compile-time path 検証 + did-you-mean、Codex 3 round 消化、commit `a8f322a`)/ **PR #59**(check / pre_commit に `is_relative_to` symlink ガード、CSCI-35b sweep #3 完了)/ **PR #60**(依存上限ピン × 5、`[tool.coverage.*]` 設定 fail_under=70、`pip-audit --strict .` プロジェクト射程化、Codex 1 round + 3.13 CI fail を `pip-audit --strict .` で同時解消)を順次 ship、本セッションで全 merged(累計 20 files, +343/-42)。**ドッグフード発見 D1〜D4**: D1 = `--package-root src` で `tests/` が観測対象外 / D2 = `syntax_error/bad.py` で extractor crash(exclude 機構欠如)/ D3 = template と user constraint の重複検出無し / D4 = **config-only PR は射程外で vacuous PASS**。**設計判断**: (a) compile-time path 検証は CompileError 化(後方非互換選択、現状壊れた path はどのみち本番で fail)、(b) `is_relative_to` 防御を 3 surface 対称化、(c) coverage `fail_under=70`(branch 73% 実測、~3pp margin)、(d) `pip-audit .` でプロジェクト射程化(env-level CVE 汚染遮断)、(e) constraint kind 別 path domain 検証(state vs delta 非対称を compile に反映)。**修正・訂正**: 私の事実誤認 2 件(would_violate docs 既明記 / `_source_fingerprint` は user input 経路ではない)、論理飛躍 1 件(原理的不可能)、PR #60 初稿 coverage rationale 誤記 + bare pip-audit 由来の 3.13 fail。**教訓**: 「リポ未確認の主張は両側に生じる」、「叩いてみて辿ったフローか?」 が抽象議論 → 実走への転換点、review iterate で kind asymmetry に深化、CSCI-35b sweep を「同梱」 ではなく「分割」 で出すと粒度が読みやすい、`is ConstraintKind.REPAIR` は False(Pydantic discriminator は plain str)、`pip-audit .` は project_path positional argument。次は (1) **target.yaml authoring guide** 新規(D1/D3/D4 を hazard 章として取り込み、半日〜1 日)、(2) 弱点 ③ **UNKNOWN を authoring/extraction に分離**(設計議論必要、1〜2 日)、(3) 弱点 A **P3a empirical alignment**、(4) D2 = extractor exclude 機構(別 brief)。**CSCI-35b sweep 残**: 2 件まで縮小(`compile_target_svp` round-trip コメント / Claude Code adapter human-friendly レンダリング)、半日 brief サイズ |
| 2026-05-07 (Session 5) | **TC10 ドッグフーディング → PR #61 merged + D5 統合**(本日 5 セッション目)。仮想 Python 包 10 ケース(TC1〜TC10)で `semantic-ci compare` / `validate-plan` / `compile-repair` を回し PASS / FAIL / REPAIR / Advisor / 入力 hardening を verdict + exit code 全 10/10 契約通りで完走。**3 つの finding** を抽出:**FINDING-1(高、未解決)** set 系 operator が `api_surface_*` / `effects` / `imports` 等の dict 要素 collection に対して **完全レコード一致**しか効かず、partial dict `{fqn: pkg.foo}` で **operator-specific な 2 種の failure mode** が発生 — false positive(`includes_all` / `includes_any` / `superset_of` / `subset_of` は実在しても violated)+ **false negative = CI bypass**(`excludes_all` は禁止記号が実在しても satisfied、forbidden-symbol policy が実質 no-op)、user constraint surface + security gate の信頼性を毀損する CI integrity gap (PR #62 follow-up で Codex review 指摘により bypass 含めて split 表記に訂正) / **FINDING-2(中、本 PR で修正)** `equals_baseline` violation 時に repair instruction の `added` / `removed` が空、`_equals_baseline` ヘルパ追加で symmetric difference を `_baseline_set_outcome` 経由 evidence に含める、設計判断ではなく sibling operator(`no_new_items` 等)との対称性漏れと判定 / **FINDING-3(低、本 PR で修正)** `compile-repair` が任意 `schema_version` を黙認、stderr warn 一行追加で前方互換性維持しつつ drift 可視化。PR #61 (`docs/dogfooding_TC10_report.md` 新規 + 修正 2 件 + テスト 5 件) を user merge(`333ee68`)。merge 後に user 依頼「FINDING-1 を Session 4 D1〜D4 計画に **D5** として統合、未解決タスクとしてドキュメント追加」 → `CLAUDE.md` 次の発行順序 §F に独立項目追加(operator semantics 変更を伴うため target_yaml_guide.md(D1/D3/D4)にも extractor exclude(D2)にも入れず別 brief、1〜2 日規模)+ `docs/dogfooding_TC10_report.md` Tracking section に D1〜D5 一覧表追加。**設計判断**: (1) FINDING-2 の Brief 化 → 本 PR 修正への格上げ(field 既存性 + sibling 対称性 + schema bump 不要 + 修正範囲の 4 軸で「設計判断ではなく実装漏れ」と判定)、(2) FINDING-3 は前方互換性壊さない warn のみ、(3) D5 は authoring hazard ではなく operator 側問題で independent brief に置くのが implementer 視点で明快。**修正・訂正**: 当初 D-numbering を CSCI-35b 4 項目の延長と取り違え `brief_5_planning.md §14` に追加しようとした → user 指示再読で Session 4 dogfood の D1〜D4 と判明、`CLAUDE.md` 次の発行順序 §F に再配置。stash pop conflict で local main divergence(21 ahead / 129 behind)に再遭遇、`git checkout -B main origin/main` で正規化(Session 3/4 と同 pattern)。**教訓**: dogfood 単独 D-numbering を立てず先行 Session の D 連番に統合する pattern は memory cross-session 連続性に効く、ユーザの「設計判断いるのか?」 問いに対して 4 軸切り分け(field 既存 / sibling 対称 / schema bump / 修正範囲)を即提示できると Brief 化 vs 即修正の判断が早い、TC2/TC5 を post-fix 再走で実地検証する習慣は dogfood の成果物検証に有効。次は (1) **D5 brief 起草**(operator semantics)、(2) target.yaml authoring guide(D1/D3/D4)、(3) extractor exclude brief(D2)、CSCI-35b sweep → D5 brief → Brief 7 のいずれの順でも可で user 判断 |
