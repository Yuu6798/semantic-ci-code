# Session Memory Index

| Date | Summary |
|------|---------|
| 2026-05-02 | P1 effect extractor 3-stage 完成 (CSCI-2/3/4): direct-call + imported-alias + global_mutation。PR #3/#4/#5 merged。`importlib.resources` 化 / TryStar 対応 / compound 文 module-scope 走査などの self-review fix を含む |
| 2026-05-03 | design.md に §17-§22 追加 (Spec Authorship / Performance Budget / Spec Quality / Suite Packaging / Vibe Coding Adapter / Multi-language Phasing) + §17/§18 を §23/§24 へリナンバー + Brief 5-7 追加。AI 時代の vibe coding 要件に対応するため Generator Adapter / Repair Compiler を P5 → P2.5 へ前倒し、TS を P3b → P2.5 並列へ前倒し |
| 2026-05-03 (Session 2) | Brief 2 完結: Python P1 抽出器 5 PR (CSCI-5〜9 / api_surface / imports / module_graph / complexity / test_surface) を 1 セッションで連続 merge。全抽出器を stdlib `ast` のみで実装 (griffe/radon/lizard/pytest --collect-only 不採用)、subprocess 跨ぎ determinism テスト共通化。CSCI-8 で Brief 内算術タイポを Codex が escalate して修正。`CodeState` 6 次元すべて populatable に。次は Brief 3 (pipeline) で CSCI-10〜14 への分割を推奨 |
| 2026-05-03 (Session 3) | Brief 3 (判定層) planning を Q1〜Q4 で確定: B (バランス) / 5 PR 細分割 / B (緩める呪文 P1 不採用) / B' (B + stable error code)。設計上の根本的気付き「詳しく書けすぎると逆にパスしてしまう」「目的と現コードがあれば仮想コードでも判定できる = generic 2-state comparator」を言語化、design.md §23 (Comparator Architecture and Application Matrix) を新設して §23/§24 を §24/§25 にリナンバー。`docs/brief_3_planning.md` 作成 (Engine API 契約 + Brief 4 申し送り含む)。`CLAUDE.md` に Session Memory ワークフローを正式追加。次は CSCI-10 Task Brief 発行から |
| 2026-05-04 | Brief 3 残務 + Brief 4 全体 (CSCI-15〜19) を 1 セッションで完結。`docs/brief_4_planning.md` 作成 (PR #20)、CSCI-15 (#21) / CSCI-16 (#22) / CSCI-17 (#23) / CSCI-17b (#24) / CSCI-18 (#25) / CSCI-19 (#26) を順次 merge、`semantic-ci` CLI 5 subcommand release 可能状態に。CSCI-16 で **self-dogfooding が `json_basic.py` 削除リグレッションを検出** (semantic-ci がツール本体の不変条件 enforcer として機能した最初の瞬間)。CSCI-17 から brief 圧縮版 (Q&A 表 + AC 1 行 + 250 行目安) に切替、品質劣化なし。`docs/cli_usage.md` / `exit_codes.md` / `json_schema.md` 整備。Open Questions 棚卸しは Brief 5 起草前に必要。次は Brief 5 (Vibe Coding Adapter / Repair Compiler) の planning から |
| 2026-05-05 | Yahoo 記事から始まり OpenMythos 解析 → semantic-ci で audit → 3 段階の実験(`vfr_vs_ifr` / `run_matrix` / `run_pregen`)で **§23.1 入力 contract「engine は state の出自を問わない」を仕様から検証済み性質に格上げ**。PR #34 で `docs/pre_generation_validation_case.md` + `experiments/pre_generation_validation/` 新設(top-level `experiments/` ディレクトリ初登場)。bot review (reproducibility 指摘) にトーンダウンではなく **self-contained 化** で対応 — 合成 baseline `tinypkg` (25 行) で外部依存ゼロの reproduction を実現。VFR/IFR / コンパス / 計器飛行の比喩が 5 ケース matrix で 5/5、最大 20× S/N 圧縮の数値として裏付け。intent と raw 差分の **直交性** が定量的に確認された。次は Tier 1 残課題(Repair loop closure / Effect-dimension drift / Allow-list 政策)+ Brief 5 planning |
| 2026-05-05 (Session 2) | PR #34 マージ後の post-merge 検証で reproduction を `/tmp/` 隔離で完走 → 実コード baseline (`src/semantic_ci_code/`) 版で 3/3 一致 → ユーザのマッチポンプ批判を受けた **adversarial test (A 真陰性 + D 偽陰性)** に拡張。A pass で「engine = fail-everything」の強い形は反証、D_real_effectful の MISS から **effects slice 実装 gap** を発見: extractor の `fqn` が callee 指向 (設計 §3.1 は enclosing function を要求)、`Path.write_text` 未検出。`docs/code_semantic_ci_design.md` §3.1 / §12 P2 と突合し **設計射程内・P2 で予定通り解消される話** と確認。実装は (1)分業ルール (2)ブランチ衛生 (3)テスト影響評価 を理由に持ち越し。Task Brief 候補 (Fix 1: extractor `fqn` semantics 修正、半日規模、P1 hot-fix) として整理。repo は無変更、検証スクリプトは全て `/tmp/real_code_validation/` に隔離。次は Brief 化判断 + adversarial test の `experiments/` 化検討 |
| 2026-05-05 (Session 3) | Brief 4b cache / 性能 slice を CSCI-25 / 26 / 27 の 3 brief 連続発行で完結。CSCI-25 (#33): `--mode {smoke,full}` (smoke = api_surface + imports + effects 3 次元、 partial state は evaluator で SKIPPED routing、 schema v1→v2 bump で `mode` + `summary.skipped` 追加)。CSCI-26 (#37 + follow-ups): `check` の CodeState cache、 Option A (CodeState cache) を Option B (worktree) より採用、 cache key core は `git rev-parse <commit>:<package_root>` の subtree object id、 corrupt は miss 扱い、 schema bump なし。CSCI-27 (本セッション merge): pre-commit cache (`git write-tree` で staged を SHA 化) + JSON cache stats (schema v2→v3) + size-based eviction (LRU by mtime, 100 MiB default) を 1 brief に同梱、 cache 系を leave 可能状態に。本体は Brief 5 (Vibe Coding Adapter) に戻る判断、 cache 残作業 14 項目は CSCI-28+ で Brief 5 後に再評価。brief 圧縮テンプレ (Q&A 表 8 セクション固定) を 3 連続実証 |
| 2026-05-05 (Session 4) | adversarial test (A 真陰性 + D 偽陰性) で effects slice 実装 gap 発見 → Brief 3/4 残課題の **再分配**(Brief 4b/4c/4d 並列 + Brief 5/6 並列 + P2 細目化)を docs に永続化、**P1 完走**(Brief 4b/4c/4d 全 merged)→ **Brief 5 (P2.5 entry) planning** まで 1 セッションで一気通貫。merged: PR #35 (status visibility) / PR #36 (redistribution) / PR #39 (Brief 4b planning) / PR #40 CSCI-28 (SARIF + GH Actions + pre-commit manifest) / PR #42 CSCI-29 (effects fqn fix) / PR #43 CSCI-30 (init + authorship + severity routing) / PR #44 (Brief 5 planning、Codex bot review fix 込み)。Brief 4d は planning doc なしで Task Brief 直接出力 pattern を確立、brief size に応じて pattern 可変化。redistribution table (§25) で「Brief 7+ deferred 押し込み」 を「行先明示」 に変換、忘却ゾーンを消した。次は Brief 6 planning(TypeScript、Brief 5 と並列発行可)+ CSCI-31 Task Brief から |
| 2026-05-06 | Responsibility Boundary を engine contract として確立。 user の「target.yaml 取り扱いがぶれて見える」 観測 → 7 件具体問題抽出 → user 原則言明「declared intent から決定論的、 瑕疵・逸脱・真意は判定外、 補正誘導・入力補足は検討価値あり」 → §23.3 を §23.1 (state 側) の **intent 側鏡像**として新設、 4 surface 模型 (Validator / Authoring / Provenance / Advisor) で既存機能を分類、 CLAUDE.md Scope guard を「This is not」 denial 形で 2 行拡張 (`not an intent validator` + `not an intent interpreter`)。 commit `fa5f887` 8 ファイル 1 PR で merged。 既存 §23.3/§23.4 を §23.4/§23.5 にリナンバ + cross-ref 2 ファイル追従。 brief_5_planning.md §4.1.1 で Adapter render-not-translate invariant を要件化。 Codex 不在で brief 発行なし、 docs only 軽量セッション pattern が成立。 次は A (code 側追従 small brief: evaluator INFO 明示分岐 + init template コメント + authorship 不可侵 invariant test) → B (CSCI-31 Brief 5 entry) → C (Brief 6 TS planning) |
| 2026-05-06 (Session 2) | Issue #48 (Semgrep を core に深く統合する提案) を Brief 化前 audit。 Semgrep 1.161.0 で決定論性 8 項目を実機検証、 5 重大発見 (OSS fingerprint 不在 / line-based fp 不安定 / path & rule_id 正規化必須 / paths.scanned 突合必須 / airgap flag 群固定) で当初前提崩壊 → core 統合 reject、 別 protocol **SSP (Semantic Security Protocol)** として §20.1 layered distribution の 4 層目に分離する設計に転換 (PR #50 で `design.md §20.1` 自体を 3 層 → 4 層に更新済み、 `semantic-ci-ssp` を suite と並列追加)。 6 論点を直列確定: Q1=B (SAST+SCA) / Q2=A (Python only) / Q3=B (**5 要素 fp** = `rule_id × module_path × qualified_name × normalized_text × ordinal`、 canonical JSON injective encoding、 ordinal は 4-tuple group → source-span 昇順 sort → 0-indexed で pin) / Q4=B+仮想コード対応 (Sensor Provenance Invariant、 §23.1 の SSP 鏡像、 4 mode = real/staged/virtual/contract-test) / Q5=A (#48 close + 新規 tracking) / Q6=A (SSP のまま、 NIST SSP 衝突は許容)。 Q3 fp 算法は本セッション当初は 4 要素 + delimiter 連結だったが、 PR #50 review で **3 段階に refine** された (詳細は `docs/brief_7_planning.md §4.3` 設計改訂履歴 callout)。 Q2 詰め中に user が **Brief 6 (TypeScript) 凍結** を表明、 SSP 議論と一体で確定 → 順序は **Brief 5 → Brief 7** に再編。 PR #49 (元は Brief 4b/4c/4d status refresh の小 PR) は merge 済み、 PR #50 (本 PR、 SSP planning + Brief 6 凍結 + 上記 fp refine + §20.1 4 層化) は review 中: `docs/brief_7_planning.md` 新設 (12 節、 6 論点確定 + audit invariants + CSCI-36〜40 候補 + 次セッション着手 checklist) + design.md §12 P3b 凍結注記 + §20.1 4 層化 + §25 表更新 (Brief 6 打ち消し / Brief 7 行追加 / Brief 7+ deferred → Brief 8+ rename) + CLAUDE.md Current Status (Brief 5 → 7 順序 + Brief 6 凍結) + docs table (brief_7_planning.md 行、 5 要素 fp + provenance 注記)。 audit fixtures `/tmp/sci_audit/` は本セッションで永続化せず Brief 7 着手時の判断に持ち越し。 **Issue #48 はクローズ済み** (state_reason=`completed`、 SSP 文脈移動 redirect コメント付き、 #48 → PR #50 で track)。 「Codex を起動できない日でも進められる docs / planning セッション」 が 2 連続で成立。 次は (1) PR #50 merge、 (2) Brief 5 (CSCI-31) 発行、 (3) Brief 5 完了後に Brief 7 (CSCI-36) 発行、 (4) **Brief 7 着手時の必読**: `docs/brief_7_planning.md §4.3` 設計改訂履歴 callout で fp 算法の最終形を確認 |
