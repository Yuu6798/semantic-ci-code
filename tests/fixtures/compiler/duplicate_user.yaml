intent: duplicate user ids
change:
  primary_kind: feature
constraints:
  - id: duplicate
    kind: delta
    target: imports
    operator: equals_baseline
  - id: duplicate
    kind: delta
    target: effects
    operator: equals_baseline
